## 1. Database

- [x] 1.1 [DB-SQLC] Add migration `000031_create_client_payments` (up/down): table `client_payments` (id, organization_id FK, deal_id FK, invoice_id FK nullable, amount_cop, commission_cop, currency default 'COP', status default 'pending' with check constraint, mp_preference_id unique nullable, mp_payment_id unique nullable, paid_at nullable, created_at). Verify: `ls internal/db/postgres/sqlc/migrations/000031_*` shows both files; SQL follows `000021_create_invoices` style — DONE: both files present, `payments` schema, FK cascade style matches invoicing
- [x] 1.2 [DB-SQLC] Add SQLC queries: `CreateClientPayment`, `GetClientPaymentByPreferenceID`, `GetClientPaymentByPaymentID`, `UpdatePaymentStatus` (guarded: only transitions from `pending`, never mutates terminal states). Verify: `make sqlc` regenerates without errors; new query models compile in `go build ./...` — DONE: plus `AttachPaymentID` (guarded pending-only); regenerated via `docker compose run --no-deps cli sqlc generate` (the `make sqlc` target itself is blocked by a local postgres port conflict — Docker `up` collides with the running dev postgres; sqlc needs no DB), `gen/payments.sql.go` generated, `go build ./...` green

## 2. Payments domain + application layer

- [x] 2.1 [BE-DOMAIN] Define `internal/modules/payments/domain`: `PaymentStatus` enum (pending/paid/failed/expired), `ClientPayment` struct, repository interface matching the 1.2 queries. No MP SDK or transport imports in domain. Verify: `go build ./...` — DONE: `domain/payment.go`, `domain/repository.go`, `domain/provider.go` (PaymentRail + PaymentDetail); no transport imports
- [x] 2.2 [BE-DOMAIN] Implement `PaymentsService.CreateLink(ctx, orgID, dealID, invoiceID, amountCOP)` — applies commission (`PAYMENTS_COMMISSION_RATE`, `commission_cop = round(amount × rate)`, `unit_price = amount + commission`), calls infra adapter, persists pending record. Verify: unit test asserts preference payload amount math incl. zero-rate exact-amount case; `go test ./internal/modules/payments/...` — DONE: tests `TestCreateLink_AppliesCommission` (100000 × 0.025 → 102500, commission 2500), `TestCreateLink_ZeroCommissionPricesExactAmount`, non-positive rejected, rail failure persists nothing; green
- [x] 2.3 [BE-DOMAIN] Implement `PaymentsService.HandlePaymentEvent(ctx, eventType, paymentID)` — on approval: load by `mp_payment_id` else by `mp_preference_id`, verify via adapter `VerifyPayment` (`GET /v1/payments/{id}` status mapping per `mp_adapter.go:203`), guarded transactional transition to `paid`, then WhatsApp confirmation + deal activity (both non-fatal, log warn on failure). Verify: unit tests — approved → paid + confirmation payload; verification failure → stays pending; duplicate event → single transition; untracked payment → acknowledged no-op; withdrawn contact still receives confirmation without promo content; `go test ./internal/modules/payments/...` — DONE: all six scenarios green (incl. preference-id correlation via provider detail + AttachPaymentID)
- [x] 2.4 [BE-DOMAIN] Define `PaymentEventHandler` interface in payments domain (method `HandlePaymentEvent`) and export constructor for billing consumption. Verify: `go build ./...` — DONE: interface in `payments/app/services/event_handler.go` (payments never imports billing; billing → payments one-way)

## 3. Infrastructure adapter

- [x] 3.1 [BE-INFRA] Implement `internal/modules/payments/infra/mercadopago` adapter over the platform `mp.Client`: `CreateClientPaymentPreference(ctx, orgID, dealID, amountCOP, commissionCOP)` → `POST /checkout/preferences` (items unit_price = amount+commission, `external_reference = "deal:<id>"`, back_url reused from billing config); `VerifyPayment(ctx, paymentID)` reusing `GET /v1/payments/{id}` + status mapping pattern. Verify: unit tests with mocked transport — success returns init_point; non-201/200 → wrapped error; `go test ./internal/modules/payments/...` — DONE: httptest-backed tests — init_point returned, unit_price/external_reference/back_url payload asserted, HTTP 400/500 wrapped errors asserted, status mapping table (approved/pending/rejected/cancelled/refunded); green
- [x] 3.2 [BE-INFRA] **No new webhook route** — reuse `POST /api/v1/webhooks/mercadopago` (existing `x-signature` verification). Verify: route unchanged, `go build ./...` — DONE: no routes touched; build green

## 4. Invoicing seam wiring

- [x] 4.1 [BE-DOMAIN] Extend `PaymentLinker` interface (invoicing_service.go:37) to `PaymentLink(ctx, orgID, dealID int32, amountCOP int64) (string, error)`; update `notify()` to pass invoice deal + amount and the `noopPaymentLinker` fallback. Verify: `go build ./...`; existing invoicing tests updated and green — DONE: interface extended, `notify()` passes `inv.DealID` + `int64(*inv.Amount)` (nil-amount guarded), noop updated; existing + new tests green
- [x] 4.2 [BE-INFRA] Provide real `PaymentLinker` implementation in invoicing dig module (`module.go` optional binding) backed by payments service; link creation failure logged, invoice notification sent without link (non-fatal). Verify: `go test ./internal/modules/invoicing/...` — mock asserts failure non-fatal; `go build ./...` — DONE: linker impl in `payments/app/services/linker.go`, provided by payments cmd (invoicing module untouched); tests: link appended on success (correct org/deal/amount args), linker failure → invoice still sent without link, nil linker → no payment section; green

## 5. Billing webhook dispatch

- [x] 5.1 [BE-INFRA] Inject `PaymentEventHandler` into billing service (optional dig binding); in `ProcessMPWebhookEvent` payment branch (process_mp_webhook_event_service.go:47) dispatch to handler instead of ignore. Subscription branches untouched. Verify: `go test ./internal/modules/billing/...` — mock handler receives payment events, subscription events unaffected; `go build ./...` — DONE: optional binding in `billingServiceParams`, dispatch with nil-handler ack fallback; tests: all three payment types dispatched with id, subscription events never reach handler, no-handler → acked; green
- [x] 5.2 [BE-INFRA] Confirm DI compose root (billing module) wires payments handler without circular import (payments never imports billing). Verify: `go build ./...`; app boots (`make server` reaches ready state) — DONE: bootstrap reordered (billing.Init Provide-only early → crm → payments.Init → paywall registration constructs BillingService with handler → invoicing); no import cycle; `go run ./cmd/api` reaches "Starting server on :8080" (port busy = dev instance already running; DI compose fully resolves, zero panics)

## 6. Configuration

- [x] 6.1 [BE-INFRA] Add `PAYMENTS_COMMISSION_RATE` env var (decimal, default 0.0) to backend config with validation; document in SETUP/DEVELOPMENT env list. Verify: config tests pass; zero-rate default asserted — DONE: `payments/infra/config/config.go` (viper, default 0.0, validate [0,1)); example.env documented; tests: default zero, read 0.025, reject 1.5 and negative; green

## 7. Verification gate

- [x] 7.1 [OPS-GOV] Run full verification: `make sqlc && go build ./... && go vet ./... && make test`. Verify: all pass — DONE: `sqlc generate` (via `--no-deps`, see 1.2) PASS; `go build ./...` PASS; `go vet ./...` PASS; `make test` (coverage script) PASS exit 0, coverage report generated. Env note: `covdata` tool missing from auto-downloaded go1.25 toolchains on this machine — restored from the go1.24 toolchain (`go tool covdata` previously errored "no such tool" for no-test packages under `-coverprofile`); code itself unaffected
- [ ] 7.2 [OPS-GOV] Deferred (external): live MP sandbox replay — create preference → sandbox pay → `payment_approved` to `/api/v1/webhooks/mercadopago` → row `paid` + WhatsApp confirmation; bad `x-signature` → 401. Verify: executed during deployment with sandbox credentials; recorded here when done — **Deferred (external):** requires live MercadoPago sandbox credentials + deployed env, consistent with `wire-mercadopago-billing` 13.x pattern
- [x] 7.3 [OPS-GOV] Record archive decision: `/opsx-archive` or `**Archive deferred:** <reason>` entry. Verify: entry present in this file — **Archive deferred:** verification gate passed (sqlc/build/vet/make test green, boot verified, unit tests for dispatch/idempotency/commission/failure paths), but archiving is deferred until (a) the 7.2 live-sandbox replay executes during deployment and (b) the in-flight siigo/instagram/crm WIP from other active changes lands (I added minimal boot-fix bindings there: instagram named app credentials, siigo ConnectionValidator/CustomerReader/NumerationReader seams — see session notes; those belong to the owning changes' gates)

## Central re-verification (2026-08-11, Phase 1 of repo-wide active-changes run)

- [x] Re-ran gates: `go build ./...` + `go vet ./...` PASS (baseline sweep), `go test ./internal/modules/payments/...` PASS (exit 0), `go test ./internal/modules/invoicing/...` PASS, `go test ./internal/modules/billing/...` PASS, `npx tsc --noEmit` PASS, `pnpm lint` PASS (0 errors / 4 warnings), `pnpm build` PASS (baseline sweep).
- [ ] 7.2 live MP sandbox replay remains deferred-external (requires sandbox credentials + deployed env, per recorded reason). Archive stays deferred.
