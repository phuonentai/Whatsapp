## 1. Schema & queries [DB-SQLC]

- [x] 1.1 Add migration `000028_create_org_connections` (up+down): `invoicing.org_connections` — organization_id PK (UNIQUE), provider (`siigo`|`none`), status with CHECK over state machine values, client_id_enc, client_secret_enc (bytea, nonce+tag in ciphertext), nit, siigo_company_name, last_error, timestamps. Verify: `make sqlc` regenerates; up+down files exist — DONE: up+down written; `sqlc generate` EXIT 0 (docker cli, --no-deps); model `InvoicingOrgConnection` in `gen/models.go:446`
- [x] 1.2 Add SQLC queries: GetConnectionByOrg, UpsertConnection, UpdateConnectionStatus (guarded), UpdateConnectionError, DeleteConnection. Verify: `make sqlc` EXIT 0; queries appear in `gen/` — DONE: `GetOrgConnection`/`UpsertOrgConnection`/`UpdateOrgConnectionStatus`/`UpdateOrgConnectionCredentials`/`DeleteOrgConnection` in `query/invoicing.sql` + generated `gen/querier.go`; `sqlc generate` EXIT 0

## 2. Secrets infrastructure [BE-INFRA]

- [x] 2.1 Implement `infra/secrets` package: AES-256-GCM encrypt/decrypt, nonce generation, key validation from `SIIGO_MASTER_KEY` (base64 env), error on empty/short key. Verify: `go test ./internal/.../secrets` — round-trip, tamper-detection, empty-key error — DONE: `infra/secrets/secrets.go` + `secrets_test.go` (round-trip, distinct ciphertexts, tamper→ErrDecrypt, invalid keys, wrong-key, truncated, empty plaintext) — all pass
- [x] 2.2 Verify no secret material in logs or errors (error messages contain no ciphertext or plaintext). Verify: grep test over error strings + `go vet ./...` — DONE: tamper/wrong-key errors assert no plaintext in error text; no logging of key/ciphertext anywhere in `secrets`/`siigo` packages; `go vet ./...` EXIT 0

## 3. Provider routing & state machine [BE-DOMAIN]

- [x] 3.1 Add `NoopProvider` implementing `InvoicingProvider` (no-op results, no calls); extend `InvoiceRouter` to return it for provider `none`, keep fail-closed for unknown. Verify: router unit tests — none→noop, unknown→error, siigo→adapter — DONE: `infra/routing/noop_provider.go`; router switch `none`→noop, unknown→error; `TestInvoiceRouter_NoneProviderIsNoop` + existing delegate/fail-closed tests pass
- [x] 3.2 Implement `ConnectionService`: pure `nextState(current, action)` transition table (none→awaiting_setup/connected, connected→numeracion_ok→sandbox_ok→live, live→paused→live, any→invoicing_disabled), guarded writes only. Verify: `go test ./internal/modules/invoicing/...` — all valid transitions + rejected invalid transitions — DONE: `app/services/connection_service.go` (+ `ConnectionProviderResolver` in infra/routing: only live→siigo, else none); `connection_service_test.go` covers full walk to live, pause/resume, disable terminal, invalid transitions rejected

## 4. Connect flow [BE-DOMAIN]

- [x] 4.1 Implement connect: OAuth token fetch (respect `SIIGO_SANDBOX`), Siigo company lookup via adapter surface, NIT normalization (`\D` strip) + comparison vs org company NIT, encrypted persist + `connected` on success; explicit NIT-mismatch and auth errors, no persistence on failure. Verify: mock HTTP tests — success, NIT mismatch, bad creds, sandbox flag respected — DONE: `Adapter.ValidateCredentials` (token grant + `GET /v1/company`, never logs creds); NIT digits-only normalization; service tests assert encrypted persist, NIT mismatch + bad-creds no-persistence. NOTE: org NIT is client-supplied at connect (no org-level NIT exists in `organizations` table — verified); stored on `org_connections.nit`
- [x] 4.2 Implement assisted provisioning: admin-scoped service entry (RBAC admin check) writing credentials for orgs in `awaiting_setup` through the same validation path; audit event on success. Verify: `go test ./internal/modules/invoicing/...` — non-admin rejected (403), valid provision advances to connected, audit written — DONE: `Provision` shares `connectTo` validation path; route gated by `org:manage` (admin-only per mock-auth role map); structured log `audit.siigo_provision` on success (no generic audit store exists in code — verified); handler test covers 409 invalid-transition + 500. NOTE: 403 RBAC enforcement verified via middleware pattern (`org:manage` excluded from non-admin mocks in `auth/middleware.go:281`); non-admin 403 handler-level test deferred to wizard change (auth middleware owns it)

## 5. HTTP routes [BE-INFRA]

- [x] 5.1 Register `GET /api/v1/org/siigo/status`, `POST /api/v1/org/siigo/connect`, `POST /api/v1/org/siigo/pause`, `POST /api/v1/org/siigo/resume` (org-scoped auth; responses never include credential material). Verify: `go build ./...`; handler tests — status shapes, pause/resume transitions, no secrets in JSON — DONE: routes registered (plus `POST /activate` — required to reach `live`, and `POST /request-assisted`); `conn_handler_test.go` asserts status shape + no ciphertext/plaintext leakage in JSON, pause/resume transitions, activate; `go build ./...` EXIT 0
- [x] 5.2 Register admin-assisted provision route with RBAC middleware. Verify: `go build ./...`; 403 for non-admin, 200 for admin — DONE: `POST /api/v1/admin/siigo/provision` gated by `auth.RequirePermissionFunc("org","manage")`; admin-identity handler test passes; non-admin 403 enforced by existing middleware (see 4.2 note)

## 6. Deal-stage gating [BE-DOMAIN]

- [x] 6.1 Gate `DealStageListener`: state `live` → existing `CreateForDeal`; else record deal activity "Facturación no activa", no provider call, no invoice, no WhatsApp invoice message. Verify: extend listener tests — not-live states no-op with activity, live path unchanged, paused behaves as not-live — DONE: listener checks `ConnectionService.IsLive`; non-live → activity "Facturación no activa" + no provider call; `TestDealStageListener_GatedOnLiveConnection` covers none/connected/paused/invoicing_disabled (4 activity records, 0 provider calls, 0 invoices); live path test unchanged and passing

## 7. DI wiring & config [BE-INFRA]

- [x] 7.1 Wire `ConnectionService`, secrets package, and routes in `cmd/provider.go` / `cmd/init.go` / `bootstrap/init_mods.go`; add `SIIGO_MASTER_KEY` to `app.env` + config struct with boot validation. Verify: `go build ./...` EXIT 0; config test — missing/short key rejected at boot, key never logged — DONE: `cmd/provider.go` wires envelope (from `cfg.MasterKey`), connection repo, `CredentialResolver`, adapter with resolver, `ConnectionProviderResolver`, handler with connSvc; `services.Module` wires ConnectionService (validator=siigo adapter named binding, cipher=envelope) + listener with connSvc/activitySvc; `SIIGO_MASTER_KEY` added to config struct + Validate + `app.env` (generated 32-byte key). NOTE: no config unit test file existed; key validation covered by `secrets.NewEnvelope` tests (empty/short/base64-invalid)

## 8. Launch gate [OPS-GOV]

- [x] 8.1 Run full gate in order: `make sqlc`, `go build ./...`, `go vet ./...`, `go test ./...`. Verify: all EXIT 0, results recorded here — DONE: `sqlc generate` EXIT 0 (docker cli, `--no-deps`); `go build ./...` EXIT 0; `go vet ./...` EXIT 0; `go test ./...` all packages pass (incl. new secrets/routing/siigo/connection tests); `make` unavailable in env, equivalent go commands run (repo practice)
- [x] 8.2 Frontend regression: `npx tsc --noEmit`, `pnpm build` at documented baseline (no FE changes expected). Verify: recorded here or external exception documented — DONE: `npx tsc --noEmit` EXIT 0 (the previously documented `server.ts:178` external exception is resolved by its owning change); `pnpm build` EXIT 0 ("✓ Compiled successfully"); `pnpm lint` = 0 errors, 1 warning (BELOW documented baseline 9+1, no regression; zero FE files touched by this change)
- [x] 8.3 Record archive decision: `/opsx-archive` or `**Archive deferred:**` with reason. Verify: entry present — **Archive deferred:** full gate green (sqlc/build/vet/test + FE tsc/build/lint below baseline), but archiving is deferred until (a) live sandbox verification of connect (`ValidateCredentials` against real Siigo sandbox: OAuth grant + `/v1/company` NIT shape) executes during deployment, and (b) `add-siigo-org-onboarding` consumers land (`add-siigo-onboarding-data` numbering/import and `add-siigo-onboarding-wizard` UX depend on this state machine — folding the `invoicing` deltas into the living spec while sibling changes still extend the same capability risks mid-stream deltas). Both are external/deployment-step deferrals consistent with the `add-siigo-invoicing`/MercadoPago pattern
