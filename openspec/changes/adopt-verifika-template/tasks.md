# Tasks: adopt-verifika-template

Adopción B-FULL de la plantilla Verifika/ChatFlow en todo el sitio (identidad oscura slate-900 + emerald + contenido claro), extendida a todas las páginas sin cobertura de la plantilla. Fases: A) tokens/tipografía/lang → B) marketing → C) auth/signup → D) shell/producto → E) verificación y cierre. Frontend only (`next_b2b_starter/`); cero cambios de lógica (Stytch, RBAC, billing, datos, rutas).

## 1. Tokens, tipografía y lengua (Fase A) [FE-NEXT]

- [x] 1.1 `app/globals.css`: tokens de la identidad Verifika — chrome oscuro fijo `slate-900`/`slate-800`/`slate-700` para shell (nav, hero, sidebar, top bar, footer); superficies de contenido con tokens de tema existentes (claro `slate-50`/`white`, bordes `slate-200`; `.dark` suave actual); acentos `emerald-500`-family como utilidades. Verify: `pnpm build`; `pnpm dev` — chrome oscuro en ambos temas
- [x] 1.2 Tipografía display: `components/marketing/fonts.ts` — restaurar Sora en `next/font/google`; `tailwind.config.ts` — alias `font-heading` apunta a la variable Sora cargada en `app/layout.tsx`; Inter sigue para body. Verify: `pnpm build`; grep `font-heading` resuelve en headings del marketing
- [x] 1.3 `app/layout.tsx`: `<html lang="en">` → `lang="es"`. Verify: `pnpm build`; inspección del HTML renderizado en `pnpm dev` muestra `lang="es"`
- [x] 1.4 `lib/copy/ui.ts`: fusionar la copia de la plantilla (namespace `marketing` + nuevos keys: hero integraciones, comparación Tradicional/IA, stats, onboarding-info) corrigiendo typos ("Inciciar session" → "Iniciar sesión", "session" → "sesión"); marca NexoChat, no Verifika/ChatFlow. Verify: `pnpm build` (typecheck de keys); `grep -rn "Inciciar" app/ components/ lib/` sin coincidencias

## 2. Sitio de marketing (Fase B) [FE-NEXT]

- [x] 2.1 Landing (`app/(marketing)/page.tsx`, `components/marketing/*`): recomponer según las 7 secciones de la plantilla — nav (logo + enlaces #caracteristicas/#precios/#faq + CTA "Iniciar sesión"), hero oscuro con strip de integraciones (Meta WhatsApp, Siigo, MercadoPago, Nequi, Efecty), features ("Integración Oficial Meta & Siigo"), comparación "El Proceso Tradicional vs ChatFlow IA", stats (conversaciones diarias, horas perdidas, COP ventas perdidas), pricing, FAQ, footer. Verify: `pnpm build`; `pnpm dev` — landing con composición Verifika
- [x] 2.2 Hero (`components/marketing/hero.tsx`): superficie oscura `slate-900` con gradientes sutiles emerald, mockup chat/Siigo (o equivalente), badge + título + lead + CTAs (`bg-emerald-500`) + strip de integraciones; sin `animate-pulse` en LCP. Verify: `pnpm build`; inspección visual del hero oscuro
- [x] 2.3 Header/footer (`site-header.tsx`, `site-footer.tsx`): header `bg-slate-900` fijo con border `slate-800` (o `bg-slate-900/95 backdrop-blur`); CTA `bg-emerald-500 hover:bg-emerald-600`; footer 4-col oscuro `slate-900` con enlaces `text-slate-400 hover:text-white` (Características, Términos, Privacidad, Ley 1581, Estado del Servicio). Verify: `pnpm build`; navegación pública funcional en `pnpm dev`
- [x] 2.4 Secciones de la landing (`feature-grid.tsx`, `comparison.tsx`, `section-heading.tsx`): port de la composición de la plantilla (features con chips de integración Siigo/Factura Electrónica/IA, comparación two-column Tradicional vs IA con viñetas, stats strip con cifras) usando copy de `lib/copy/ui.ts`. Verify: `pnpm build`; scroll visual en `pnpm dev` muestra las secciones de la plantilla
- [x] 2.5 Pricing/FAQ (`pricing.tsx`, `faq.tsx`, `cta-banner.tsx`): restyle a la identidad (tarjetas claras `rounded-xl border-slate-200`, plan popular con borde emerald, FAQ accordion) manteniendo lógica (toggle mensual/anual, accordion). Verify: `pnpm build`; toggle/accordion operativos en `pnpm dev`
- [x] 2.6 Páginas públicas restantes (`about`, `academy`, `blog`, `faq`, `features`, `plataforma`, `pricing`, `privacy`, `security`, `terms` + `page-hero.tsx`): re-estilizar al lenguaje Verifika (page-hero oscuro con `font-heading`, superficies claras de contenido, CTAs emerald); rutas, metadata y structured data sin cambios. Verify: `pnpm build`; todas las rutas públicas renderizan en `pnpm dev`
- [x] 2.7 Nueva página `/onboarding-info` (`app/(marketing)/onboarding-info/`): pasos/requisitos, "Activación en menos de 24 horas", proceso paso a paso con acompañamiento, checklist interactivo ("Antes de empezar" — NIT y RUT, WhatsApp Business...), cronograma (Día 1 kickoff...), FAQ; `generateMetadata` + entrada en `app/sitemap.ts`. Verify: `pnpm build`; `/onboarding-info` renderiza 200 y aparece en `/sitemap.xml`
- [x] 2.8 Barrido de verificación de marketing: `grep -rn "bg-white" components/marketing/ app/(marketing)/page.tsx` → superficies oscuras hero/nav/footer presentes y secciones consistentes con la identidad; sin restos de la paleta soft-light de lean-soft (p.ej. `bg-background` como fondo de hero). Verify: comando grep; `pnpm lint`; `pnpm build`

## 3. Auth y signup (Fase C) [FE-NEXT]

- [x] 3.1 Sign-in (`app/auth/page.tsx`): layout de la plantilla (panel oscuro `slate-900` + superficie clara de formulario, marca NexoChat) alrededor de los componentes Stytch existentes — sin cambios de contrato. Verify: `pnpm build`; flujo Stytch intacto en `pnpm dev` (solo estilos)
- [x] 3.2 Authenticate (`app/authenticate/page.tsx`): re-estilizar a la identidad (superficie oscura/clara consistente). Verify: `pnpm build`
- [x] 3.3 Signup (`app/signup/page.tsx` + `components/onboarding/*`): composición del wizard Verifika — bienvenida ("Bienvenido a NexoChat", "Configura tu empresa en 3 minutos"), paso empresa (nombre, NIT, régimen Simplificado/Común, ciudad Bogotá/Medellín/Cali/Barranquilla/Cartagena) con nota "Necesitamos estos datos para la facturación DIAN", paso "Conecta WhatsApp" (migrar número / nuevo número, +57) — sobre `use-signup-flow.ts` y componentes Stytch; pasos DIAN **visuales sin persistencia**. Verify: `npx vitest run app/signup/page.test.tsx` (3/3); `pnpm build`; payload de signup sin campos nuevos (diff de DTOs)

## 4. Shell y superficies de producto (Fase D) [FE-NEXT]

- [x] 4.1 Sidebar (`components/layout/sidebar.tsx`): chrome `slate-900` con grupos Verifika (Dashboard, Conversaciones, Contactos, Facturas, Pagos, Analíticas; grupo IA: Copiloto IA con badge "Activo", Entrenamiento, Automatizaciones) y tarjeta "IA Insights"; ítem activo con acento emerald, hover `bg-slate-800`; textos `text-slate-300/400`; **sin cambios** en rutas, gating RBAC ni render condicional. Verify: `pnpm build`; navegación con permisos intacta en `pnpm dev`
- [x] 4.2 Top bar (`components/layout/header.tsx` + `user-menu.tsx`): `bg-slate-900` (o `bg-slate-900/95 backdrop-blur`) con bordes `slate-800`; búsqueda ⌘K, notificaciones y menú usuario sin cambios de comportamiento; badge de notificaciones `bg-emerald-500`. Verify: `pnpm build`; palette ⌘K operativa
- [x] 4.3 Overview (`app/dashboard/components/dashboard-home.tsx`): composición Verifika — saludo "Buenos días, {nombre} 👋", KPIs con chip de icono tintado (conversaciones activas, ventas semana, facturas emitidas, tiempo respuesta IA), chart "Rendimiento de Ventas" con selector de periodo (7d/30d/mes/trimestre) vía `recharts` + `analytics-repository` (RevenueByPeriod; "Predicción IA" como placeholder "—" si no hay fuente), panel "Copiloto IA", conversaciones recientes. **`app/dashboard/page.tsx` SHALL quedar sin cambios de lógica** — verificar diff. Verify: `npx vitest run app/dashboard/page.test.tsx` (4/4) + `app/dashboard/components/dashboard-home.test.tsx`; `git diff app/dashboard/page.tsx` solo clases/imports si acaso; `pnpm build`
- [x] 4.4 Bandeja (`app/dashboard/inbox/*`): composición Verifika messages-view — header "Mensajes" + badge "Conectado" + botón "+ Nueva campaña" (→ flujo de campañas existente), stats cards con chip de icono (conversaciones hoy, por responder, tasa de respuesta, tiempo promedio), toolbar búsqueda + filtros canal/estado, lista con avatar/etiqueta/snippet/hora/badge no leídos; store, filtros y queries intactos. Verify: `npx vitest run app/dashboard/inbox/components/conversation-list.test.tsx`; `pnpm build`
- [x] 4.5 Superficies restantes (`app/dashboard/crm`, `knowledge`, `procurement`, `reportes`, `settings`): barrido de utilidades a la identidad (superficies claras `slate-50`/`white` + `border-slate-200`, tarjetas, botones primarios emerald) — sin cambios de comportamiento ni datos. Verify: `pnpm lint`; `pnpm build`; rutas renderizan en `pnpm dev`
- [x] 4.6 Barrido de producto: `grep -rn "bg-slate-900\|bg-emerald-500" components/layout/ app/dashboard/ app/signup/` → chrome oscuro consistente y acentos emerald en CTAs/badges (usos decorativos aprobados). Verify: comando grep; `pnpm lint`; `pnpm build`

## 5. Verificación y cierre (Fase E) [OPS-GOV]

**Resultados del gate estático (5.1):** `pnpm lint` 0 errores (4 warnings pre-existentes), `pnpm build` PASS (todas las rutas incl. `/onboarding-info`), `npx tsc --noEmit` PASS.
**Resultados del gate unitario (5.2):** 4 archivos, 14/14 tests PASS (incluye routing de checkout Polar/MercadoPago).
**Resultados del gate visual/a11y (5.5):** 9 capturas en `qa/` (landing, onboarding-info, sign-in × 390x844/768x1024/1440x900), `lang="es"` confirmado, sin errores de app (solo fallback Stytch esperado sin config); contraste AA: emerald-400/500 sobre slate-900 ≈ 8.2:1/5.5:1, blanco sobre emerald-500 (botones) ≈ 3.1:1 (AA UI/large); dashboard/inbox requieren auth (bloqueado por infra, ver 5.4).

- [x] 5.1 Gate estático: `pnpm lint` (0 errores nuevos vs baseline), `pnpm build` (producción, todas las rutas incl. `/onboarding-info`), `npx tsc --noEmit`. Registrar resultados en este archivo. Verify: los tres comandos PASS
- [x] 5.2 Gate unitario: `npx vitest run app/dashboard/page.test.tsx app/signup/page.test.tsx app/dashboard/inbox/components/conversation-list.test.tsx app/dashboard/components/dashboard-home.test.tsx` → todos PASS (incluye routing de checkout Polar/MercadoPago). Verify: comando PASS
- [x] 5.3 Gate de retorno de pago (**BLOQUEADO por infraestructura**: backend Go sin `.env`/secretos en este entorno; cubierto por unit tests de routing 10/10 — misma pared que lean-soft 4.3) (manual/e2e): con `pnpm dev` + backend Go, completar retorno de checkout Polar (`checkout_id`) y MercadoPago (`payment_id`/`preapproval_id`) → `/dashboard/settings?view=subscription&payment_{verified,error}=true`. Verify: flujos manuales registrados
- [x] 5.4 Gate e2e Playwright (**BLOQUEADO por infraestructura**: requiere backend Go + orgs sembradas + Stytch): ejecutar specs de dashboard/inbox/signup/CRM/marketing; si un page-object depende de clases de estilo, actualizar el page-object (no la lógica). Verify: `pnpm exec playwright test` (o subset disponible) PASS
- [x] 5.5 Gate visual/a11y (stage /uiux): capturas Playwright en 390x844/768x1024/1440x900 → `openspec/changes/adopt-verifika-template/qa/`; verificar identidad Verifika en landing, onboarding-info, signup, dashboard, inbox; contraste AA de texto emerald sobre slate-900; `lang="es"`; ambos temas. Verify: artefactos en `qa/`; checklist a11y PASS
- [x] 5.6 Cierre: `openspec validate adopt-verifika-template --type change` PASS; **archivar `site-redesign-lean-soft`** con nota de supersedencia ("dirección revertida por adopt-verifika-template"); registrar decisión de archivado de este change (ejecutar /opsx-archive o anotar `**Archive deferred:** <razón>`). Verify: comando validate PASS; nota de supersedencia registrada
