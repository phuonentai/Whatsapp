# Tasks: Inbox v2 — tiers de acceso + pulido del AI rail

Tiers de acceso en la bandeja (miembro lee/responde; admin/agente todo) con enforcement server-side, y pulido del AI rail. Backend Go + política Stytch + frontend Next. Orden: política → backend → frontend → verificación.

## 1. Política y backend [BE-INFRA]

- [x] 1.1 Spike: mapear la política Stytch actual (roles, permisos de bandeja, asignación a `member`); documentar el cambio de política y su rollback (Stytch dashboard/API). Verify: notas en tasks con el mapeo rol→permiso y rollback. **Spike (notas):** política vive en Stytch (runtime SSOT; spec `stytch-authorization`: cache Redis 5 min, 503 si inaccesible). Espejo local en `internal/modules/auth/rbac.go` (`AllPermissions`, roles member/manager/admin) usado como fallback en `hasPermission`. Hoy: frontend gatea bandeja con `org:manage` (page.tsx + sidebar); endpoints `GET /crm/conversaciones*` con `contact:view`, envío con `contact:manage`. **Cambio de política:** añadir `inbox:view` + `inbox:reply` a los roles `member` y `manager` (admin conserva `org:manage`, que cubre todo vía OR); espejo local actualizado en rbac.go + frontend `permissions.ts`. **Rollback (dual):** Stytch dashboard/API → quitar `inbox:view`/`inbox:reply` de member/manager (restaurar gate `org:manage` completo); git revert. Nota operativa: la propagación del cambio/revert toma hasta el TTL de la política (5 min).
- [x] 1.2 Añadir permisos `inbox:view`/`inbox:reply` a la política Stytch y asignar a roles (miembro/agente/admin) según design. Verify: política aplicada y verificable (Stytch). **Hecho:** `PermInboxView`/`PermInboxReply` en rbac.go (`AllPermissions`); `member` y `manager` los obtienen (admin via `org:manage`). La política Stytch real se actualiza en el dashboard/API según el mapeo documentado en 1.1 (paso operativo externo; el enforcement server-side ya valida ambos permisos).
- [x] 1.3 Backend: enforcement por permiso en `GET /crm/conversaciones*` (`inbox:view`) y envío manual (`inbox:reply`); close/reopen/quick-replies/sugerencias siguen `org:manage` (403 server-side). Verify: `make test`; tests 200/403 por rol. **Hecho:** rutas: list/mensajes GET → `RequireAnyPermissionFunc(inbox:view, org:manage)`; POST mensajes → `(inbox:reply, org:manage)`; PATCH status → `org:manage`; template → `org:manage`; `GET /agent/suggestions` → `org:manage`; `POST /agent/rephrase` (writing assist) → `org:manage`; `GET /agent/conversations/:id/context` → `(inbox:view, org:manage)`. Tests 200/403 por rol en `inbox_tier_test.go`.
- [x] 1.4 Helper de autorización compartido para la bandeja (un solo punto de check por permiso). Verify: `make test`; `make lint`. **Hecho:** se usa `auth.RequireAnyPermissionFunc` (helper existente, wildcard-aware) en todos los gates de bandeja + seam `services.ManualSendGovernance` en `crm/app/services/governance.go` (un solo punto de gobierno para envíos manuales; implementado por el módulo agent vía DI sin ciclo).
- [x] 1.5 Confirmar que el envío manual de miembro atraviesa guardrails (`agent-governance`) con denials en audit — sin excepción de rol. Verify: `make test`; test de denial por miembro. **Hecho:** `agentService.GovernManualSend` (seam `ManualSendGovernance`) evalúa los mismos guardrails `send_message` (kill switch, descuento, términos prohibidos, escalación, consentimiento, ventana, límite diario) con snapshot del miembro, audita allow/deny con el actor y deniega sin side effect. `HandleSendMessage` lo usa para TODOS los actores (sin excepción de rol) y responde 403 en denial. Tests: `governance_test.go` (denial forbidden_term + audit, allow, kill switch).

## 2. Frontend — tiers [FE-NEXT]

- [x] 2.1 Sidebar: la bandeja se muestra con `inbox:view` u `org:manage`; rutas y gating de otras secciones intactos. Verify: `pnpm build`; navegación por rol. **Hecho:** `SURFACE_GATES.inbox` → `anyPermissions: [INBOX_VIEW, ORG_MANAGE]` (fuente única sidebar + settings); page gate actualizado; otras rutas intactas. `pnpm build` PASS.
- [x] 2.2 Inbox: ocultar controles admin para miembros (cerrar/reabrir, quick replies, sugerencias, writing assist) — sin ghosts; leer lista + thread + composer manual activo. Verify: `pnpm build`; vista de miembro sin controles admin. **Hecho:** `page.tsx` computa `canManage = hasPermission(org:manage)`; cierra/reabre oculto en `ConversationHeader`, QuickReplies/AgentSuggestionsPanel no se renderizan para miembros, writing assist oculto en `ReplyInput` (`showWritingAssist`); el composer manual queda activo (placeholder "Escribe un mensaje...").
- [x] 2.3 Permissions en `lib/auth/permissions.ts` (`INBOX_VIEW`, `INBOX_REPLY`) + hooks de permisos. Verify: `pnpm lint`; `npx tsc --noEmit`. **Hecho:** constantes añadidas; `usePermissions().hasPermission` ya soporta wildcard/expansión; `tsc` y `lint` PASS.

## 3. Frontend — AI rail pulido [FE-NEXT]

- [x] 3.1 Distinción visual IA: ✦ + tinte violet en drafts/sugerencias; escalaciones amber "requiere juicio humano" sin auto-send. Verify: `npx vitest run app/dashboard/inbox/components/agent-suggestions-panel.test.tsx`; `pnpm build`. **Hecho:** badge ✦ IA (violet) en sugerencias; escalaciones amber con "requiere juicio humano"; nunca auto-send. Tests actualizados PASS.
- [x] 3.2 Aprobar precarga el composer (prefill, sin envío silencioso); botón explícito de envío. Verify: `npx vitest run app/dashboard/inbox/components/reply-input.test.tsx`; `pnpm build`. **Hecho:** `AgentSuggestionsPanel.onApproveAsDraft(body)` → `setDraft(body)` en page.tsx; "Aprobar"/"Usar borrador" precargan; el envío explícito queda en el composer (path gobernado por guardrails).
- [x] 3.3 Contexto consent-gated: tarjeta estructural "mensajes: N · consentimiento retirado" sin hechos pre-generados. Verify: `pnpm build`; degradación visible. **Hecho:** panel de contexto existente (structural card) + copy actualizada (es/en) "El contacto retiró o aún no autoriza... sin hechos pre-generados"; `consentGated` incluye retirado (backend `ConversationContextService`).
- [x] 3.4 Composer 402 solo-IA: sparkles disabled + tooltip "Créditos agotados — ver plan"; composer manual funcional; aviso de una línea en panel de sugerencias. Verify: `pnpm build`; estados con y sin créditos. **Hecho:** `ReplyInput` lee `useAiUsageQuery`; sparkles disabled + tooltip + item del menú con aviso cuando `credits_remaining=0`; composer manual intacto; aviso de una línea en el panel de sugerencias. Test PASS.
- [x] 3.5 Unread: dot + bold; live-region discreta ("2 nuevos") en poll 5s sin spam. Verify: `pnpm build`; anuncio solo en incrementos. **Hecho:** lista ya tenía dot+bold (`isConversationUnread`); page añade live-region `sr-only` que anuncia SOLO incrementos del poll (refetchInterval 5000) vía `tpl(ui.inbox.newMessagesLiveRegion, {n})`.
- [x] 3.6 Banner de canal desconectado sobre el composer ("WhatsApp desconectado — los envíos fallarán") por canal seleccionado. Verify: `pnpm build`; banner visible con canal desconectado. **Hecho:** `useWhatsAppConfigQuery`/`useInstagramConfigQuery` → banner `role=status` sobre el composer cuando el canal seleccionado (o el de la conversación activa) está inactivo; copy es/en.
- [x] 3.7 Chip de secuencia "Paso k de n" sobre el input; avanza solo en envío exitoso; reset al cambiar conversación. Verify: `pnpm build`; flujo de secuencia. **Hecho:** chip usa `tpl(ui.inbox.sequenceStepChip, {k, n})` con `role=status`; avance/reset ya manejado por `useSequence` (solo tras envío exitoso, reset al cambiar conversación).
- [x] 3.8 Copy nueva en `lib/copy/ui.ts` (español tipado: "Sugerencias", "Contexto", "Paso k de n", "Consentimiento retirado — contexto limitado", "Créditos agotados"). Verify: `pnpm lint`. **Hecho:** claves es/en añadidas (`aiDraftMarker`, `escalationHumanNote`, `approveAsDraft`, `useDraft`, `creditsExhaustedTooltip`, `creditsExhaustedPanelNotice`, `channelDisconnectedBanner`, `newMessagesLiveRegion`, `sequenceStepChip`, `consentWithdrawnContext`) + copy de contexto consent-gated actualizada; `pnpm lint` PASS.

## 4. Verificación [OPS-GOV]

- [ ] 4.1 Gate backend: `make test` (incluye tests por rol y denials), `make lint`. Verify: comandos PASS
- [ ] 4.2 Gate estático frontend: `pnpm lint`, `pnpm build`, `npx tsc --noEmit`. Verify: los tres comandos PASS
- [ ] 4.3 Gate unitario frontend: `npx vitest run app/dashboard/inbox/` → todos PASS. Verify: comando PASS
- [ ] 4.4 Gate visual/a11y: capturas Playwright 390x844/768x1024/1440x900 por tier (miembro/admin) → `openspec/changes/inbox-member-tier/qa/`; contraste ✦/amber, live-region, chips con texto. Verify: artefactos en `qa/`
- [ ] 4.5 Cierre: `openspec validate inbox-member-tier --type change` PASS; registrar decisión de archivado (ejecutar /opsx-archive o anotar `**Archive deferred:** <razón>`). Verify: comando validate PASS

**Rollback de política Stytch:** documentar en tasks el comando/pasos para restaurar el gate `org:manage` completo (Stytch dashboard) junto con el revert de Git.
