# Revision 1 (2026-08-12)

- [x] item 1 — Platform request-context model: design.md Decisions 2 & 7 + risk bullet + open question (org dedicada `platform-ops`, `authorization_check` descartado para lecturas cross-org, claim `roles`/custom claim del JWT, fallback 403/503, validación de `org_id`); delta `admin-panel-surface` (requirement "Contexto de plataforma" + scenarios org_id/regresión/fallback); proposal.md (Auth bullet); tasks.md 1.1(d), 1.2, 2.1, 2.6, 2.7
- [x] item 2 — Alcance de datos cross-org acotado: design.md Decision 8 + Non-Goals + R1; delta `admin-panel-audit-log` reescrito (exclusión explícita de CRM/contactos/conversaciones, escenario "Sin datos de clientes"); delta `admin-panel-surface` (detalle de org y auditoría acotados); proposal.md (bullets de auditoría y Non-Goals); tasks.md 2.2, 2.4, 3.2, 3.4
- [x] item 3 — Spike de agregación: design.md Decision 4 + Risks + open question (cobertura de índices period-first, expand-contract); delta `ai-usage-metering` (escenario "Agregado platform-wide con paginación"); tasks.md 1.1(b)
- [x] item 4 — Auditoría de acceso concreta: design.md Decision 9 + R1 + Migration Plan paso 3 (`platform_access_log`, retención 90 días, sin ALTERs); delta `admin-panel-surface` (requirement "Auditoría de acceso de plataforma"); delta `admin-panel-audit-log` (escenario "Lecturas registradas"); proposal.md (Impact); tasks.md 1.3, 2.8

Artifacts revised in this pass: `design.md`, `proposal.md`, `tasks.md`, `specs/admin-panel-surface/spec.md`, `specs/admin-panel-audit-log/spec.md`, `specs/ai-usage-metering/spec.md`, `specs/admin-panel-navigation/spec.md` (coherencia del gate 403/503).

Residuales aceptados (sin cambio): R2 (expectativa de gestión — product, trigger definido), R3 (tasas de modelo desactualizadas — mitigado con etiqueta "referencia" + spike de derivación), degradación de estado externo → "—". Adicional: modelo de operador Stytch (org dedicada vs miembro sin org) queda como assumption a validar en spike 1.1 — grounded en docs Stytch (sesiones B2B siempre org-scoped).

# Revision 2 (2026-08-12) — decisión de producto: dos paneles, plataforma SIN Siigo

- [x] item 1 (verdict round 2, Siigo write/read-only contradiction) — RESUELTO por decisión de producto: **dos paneles con frontera estricta** — el panel del tenant (`/dashboard/settings`) ve sus datos incl. Siigo (`siigo-admin-view` permanece en settings, org-scoped, gate rol `admin`, SIN CAMBIOS); el panel de plataforma (`/admin`) NO expone NINGÚN dato de Siigo (ni sección, ni estado de conexión, ni credenciales, ni facturación). Design.md: Decision 5 reemplazada, Decision 8 y Non-Goals actualizados, riesgo e2e eliminado; delta `admin-panel-surface` (sidebar sin Siigo + requirement "Siigo fuera de alcance de la plataforma" con 3 escenarios); delta `admin-panel-navigation` (secciones Organizaciones/Uso IA/Auditoría); proposal.md (Why, What Changes, capabilities, Non-Goals, Assumptions); tasks.md (3.1 sin Siigo, 3.2 sin Siigo, 3.5 = verificación de límite + regresión siigo-admin intacta). Elimina el conflicto con `settings-redesign` y es consistente con el living spec `admin-panel-navigation` (onboarding overview gateado al rol `admin` del tenant) y con `ROADMAP.md`/`GAP-ANALYSIS-2026.md` (silenciosos sobre shell de plataforma).
- [x] item 2 (verdict round 2, acceso-log operacional) — PARCIAL: task 1.3 ampliada con job de limpieza de retención 90 días + índice `(created_at)`; la política de fallo de escritura (fail-open vs fail-closed) queda como open question en design.md (decisión en tasks 2.8 con observabilidad/alertas).

Artifacts revised in this pass: `design.md`, `proposal.md`, `tasks.md`, `specs/admin-panel-surface/spec.md`, `specs/admin-panel-navigation/spec.md`, `revision.md`.

Residuales para re-revisión del council: política de fallo de escritura de `platform_access_log` (open question, no decidida); S2 staleness del claim `roles` del JWT (revocación vía ban de Stytch, documentada en rollback); R2/R3 sin cambio; valor proposicional pasivo de la vista Uso IA (assumption de producto).
