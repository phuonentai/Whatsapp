## 1. Audit Helper

- [x] 1.1 [FE-NEXT] Create `next_b2b_starter/lib/auth/audit.ts` (server-only): `recordAuthAudit({ type, memberId, organizationId, detail })` posts a `tipo=sistema` activity row to the existing Go `POST /api/crm/actividades` using the member session JWT; payload contains only `{type, member_id, organization_id, detail}` with `detail` from a bounded enum; wraps failures in try/catch and logs `console.warn` without throwing. Verification: `pnpm lint`; `pnpm build` passes.
  - Gate 2026-08-11: `pnpm exec eslint lib/auth/audit.ts lib/auth/audit.test.ts lib/actions/auth/send-magic-link.ts lib/actions/auth/consume-magic-link.ts lib/actions/auth/logout.ts` exit 0 (no errors/warnings on changed files); full `pnpm lint` exit 0 (0 errors / 4 documented baseline warnings); `pnpm build` exit 0 (recorded under 3.1).

- [x] 1.2 [FE-NEXT] Add unit tests `audit.test.ts`: correct payload shape per event type; API failure is swallowed (no throw); no token material in payload. Verification: `pnpm test -- audit` passes.
  - Gate 2026-08-11: `pnpm exec vitest run audit` → Test Files 1 passed (1), Tests 11 passed (11). Covers: payload shape per event type (tipo=sistema, endpoint URL, Bearer header), bounded detail in payload/content, payload keys = ONLY {type, member_id, organization_id, detail}, no token material in body, network failure swallowed + warn, non-2xx swallowed + warn, no-JWT skip (no fetch + warn), out-of-enum detail dropped, `mapAuthErrorToDetail` mapping. NOTE: `pnpm test -- audit` forwards the filter as a positional arg that this pnpm version drops (runs full suite); targeted `vitest run audit` is the equivalent gate and passes. Full-suite `pnpm test` recorded under 3.1.

## 2. Wire Auth Events

- [x] 2.1 [FE-NEXT] `sendMagicLink.ts`: after a successful send (and per-org loop), record `magic_link_requested` per org. Verification: `pnpm build`; `pnpm lint` passes.
  - Gate 2026-08-11: `recordAuthAudit({type: "magic_link_requested", memberId, organizationId})` awaited in a per-org loop after `Promise.all` send succeeds (design D4 — N rows for N orgs). `pnpm lint` exit 0 (full run, 0 errors); `pnpm build` exit 0 (under 3.1). Note: `sendMagicLink` runs pre-auth, so the helper finds no session JWT in cookies, warns and skips the POST — honest best-effort behavior per spec; the recording path is exercised when a session exists (see 2.2/2.3).

- [x] 2.2 [FE-NEXT] `consumeMagicLink.ts`: record `login_succeeded` on member-authenticated success; `login_failed` on token error paths (detail = error code); `mfa_challenge_passed`/`mfa_challenge_failed` when MFA fields present. Verification: `pnpm build`; `pnpm test` passes.
  - Gate 2026-08-11: `login_succeeded` recorded after session cookies are set (fresh JWT readable by the helper's own `cookies()` read — same request-scoped mutable store); `login_failed` recorded in the catch with `detail: mapAuthErrorToDetail(error)` (bounded enum, unknown codes → `internal_error`; also refactored the pre-existing `catch (error: any)` to `unknown` + narrowing); `mfa_challenge_failed` (detail `mfa_required`) recorded when `result.mfa_required` is truthy in the `!member_authenticated` branch — the enum defines `mfa_challenge_passed` now (design D3) and its recording site lands with the MFA change (this action never verifies an MFA code, so there is no passed-challenge path here). `pnpm build` exit 0 (under 3.1); full `pnpm test` → 41 files / 197 tests passed (under 3.1).

- [x] 2.3 [FE-NEXT] `logout.ts`: record `logout` when the member session was present. Verification: `pnpm build` passes.
  - Gate 2026-08-11: `recordAuthAudit({type: "logout", memberId, organizationId})` awaited when `sessionToken || sessionJwt` is present, BEFORE session revocation (JWT must still be valid for the Go API); member/org ids extracted from the session JWT claims (`decodeJWT` + `https://stytch.com/session` narrowing, no casts). `pnpm build` exit 0 (under 3.1).

## 3. Verification Gate

- [x] 3.1 [FE-NEXT] `pnpm lint`, `pnpm build`, `pnpm test` pass.
  - Gate 2026-08-11: `pnpm lint` exit 0 — 0 errors / 4 warnings (documented baseline: react-hooks/incompatible-library ×2 contact-table + virtualizer, react-hooks/exhaustive-deps deal-kanban, @next/next/no-img-element prose.tsx). `pnpm build` exit 0 (next build, full route output). `pnpm test` exit 0 — Test Files 41 passed (41), Tests 197 passed (197). `npx tsc --noEmit` exit 0. Note: earlier intermediate full-suite/lint failures (command-palette.test.tsx "Too many re-renders", knowledge-content.tsx setState-in-effect) were PaletteFixer's in-flight edits; after they stabilized (peer-confirmed 22:03) the full suite/lint/build are green.
- [x] 3.2 [OPS-GOV] Confirm audit payloads contain no session/token material (code review); `openspec validate auth-audit-events` passes.
  - Gate 2026-08-11: code review — `recordAuthAudit` reads the session JWT only into the `Authorization` header (never into the body); body/metadata carry ONLY `{type, member_id, organization_id, detail}`; `detail` is a bounded enum (`AUTH_AUDIT_DETAILS`) with runtime guard (`isAuthAuditDetail`) + `mapAuthErrorToDetail` collapsing unknown error bodies to `internal_error`; unit test asserts no token/jwt/session/password strings in the payload. `openspec validate auth-audit-events` → "Change 'auth-audit-events' is valid" (exit 0).

**Archive ready:** all 7 tasks green — audit tests 11/11 (`vitest run audit`), full `pnpm test` 41 files / 197 tests, `pnpm lint` 0 errors, `pnpm build` exit 0, `npx tsc --noEmit` clean, `openspec validate auth-audit-events` valid. No external blockers (no credentials/deploy required; FE-only change reusing the existing Go activity endpoint). Orchestrator may archive via `/opsx-archive`.
