# Tasks: add-campaign-ai-message

## 1. Database: campaign message column [DB-SQLC]

- [x] 1.1 Add migration `000035_add_campaign_message` (up: `ALTER TABLE campaigns ADD COLUMN mensaje TEXT NULL`; down: drop column), following the `000021_create_invoices` style. Verify: `ls internal/db/postgres/sqlc/migrations/000035_*` shows both files. — DONE: `000035_add_campaign_message.up.sql` (ALTER TABLE crm.campaigns ADD COLUMN mensaje TEXT NULL + COMMENT ON COLUMN) and `.down.sql` (DROP COLUMN IF EXISTS mensaje) created; both listed; migration head is 000034 → 000035 free, per repo note.
- [x] 1.2 Update the `CreateCampaign` SQLC query in `internal/db/postgres/sqlc/query/campaigns.sql` to accept optional `mensaje`; regenerate via `docker compose run --no-deps cli sqlc generate` (or `sqlc generate` — `make sqlc` is blocked by the local postgres port conflict per repo note). Verify: generated `gen/campaigns.sql.go` contains `Mensaje`; `go build ./...`. — DONE: query now inserts `(organization_id, nombre, segment_id, created_by, mensaje) VALUES ($1..$5)`; regenerated via `docker compose -f deps/docker-compose.yml run --no-deps --rm -w /workspace/internal/db/postgres/sqlc cli sqlc generate` (the repo note's `docker compose run --no-deps cli` fails because the compose file is `deps/docker-compose.yml`); `gen/campaigns.sql.go` has `Mensaje pgtype.Text` in `CreateCampaignParams` + `CrmCampaign` scan; `go build ./...` PASS.

## 2. Backend: builder message draft + campaign create [BE-DOMAIN]

- [x] 2.1 Extend `AudienceBuildResult` (`domain/entities.go`) with `MessageDraft string` and `Campaign` with `Mensaje *string`. Extend `ai_audience_builder.go`: append message-drafting rules to `audienceSystemPrompt` (Spanish WhatsApp promo copy, tone from description, CTA, Ley 1581-compliant, no contact PII placeholders) and parse `message_draft` from the same response (fence-tolerant, per design D1/D2/D3); unparsable/empty message → omitted, never fails the call. Verify: `go build ./...`. — DONE: entities extended; prompt rewritten to a single JSON object contract `{"filter_spec": [...], "message_draft": "..."}` with draft rules (Spanish 1–3 sentences, tone from description, CTA, Ley 1581, zero PII placeholders, empty draft allowed); new `parseAIBuildResponse` (fence-tolerant, legacy bare-array accepted; message_draft dropped on parse failure/empty; spec failure still errors); `go build ./...` PASS.
- [x] 2.2 Thread optional `mensaje` through `campaign_service.go` `Create` and `campaign_repository.go` (`sqlc.CreateCampaignParams{Mensaje}`); update `handler.go` `CreateCampaign` body to accept optional `mensaje`. Old clients (no `mensaje`) create null-message drafts. Verify: `go build ./...`; `grep -n "mensaje" internal/modules/campaigns/handler.go`. — DONE: interface + service + repo Create signatures carry `mensaje string` (trimmed; empty → NULL via `pgText`); `mapCampaign` maps `Mensaje` (nil when NULL); handler body gains `Mensaje string json:"mensaje"` (optional); handler grep shows `Mensaje` binding + create call; `go build ./...` PASS.
- [x] 2.3 Unit tests: `ai_audience_builder_test.go` — Build returns `message_draft` alongside filter spec; message unparsable → 200 with spec, no message; filter spec invalid → 400 unchanged; credits exhausted → 402, no LLM call; metered recording. `campaign_service_test.go` — create persists mensaje; create without mensaje works. Verify: `go test ./internal/modules/campaigns/...` — all pass. — DONE: mockLLM counts calls; new tests `TestAiBuildHappyPath` (asserts MessageDraft + exactly 1 metered LLM call), `TestAiBuildMessageDraftFencedJSON`, `TestAiBuildUnparsableMessageDropped` (spec kept, draft dropped), `TestAiBuildEmptyMessageDropped`, `TestAiBuildCreditsExhaustedFailClosed` (0 LLM calls); existing invalid-spec/non-JSON/tag-org/empty tests unchanged and green; service tests `TestCreateCampaignPersistsMensaje` + `TestCreateCampaignWithoutMensaje` added, mock repo Create updated; `go test ./internal/modules/campaigns/...` PASS exit 0.

## 3. Frontend: message field + AI prefill [FE-NEXT]

- [x] 3.1 Add an optional message textarea to the campaign create form in `components/crm/campaign-manager.tsx`; after a successful `aiBuild`, pre-fill it with `message_draft` (editable; nothing auto-saved or sent); include `mensaje` in the `createCampaign` payload when non-empty. Verify: `pnpm lint`; `npx tsc --noEmit`. — DONE: textarea (`campana_mensaje`, aria-label + placeholder from copy) in the create form; `handleAiBuildSuccess` pre-fills `campaignMensaje` from `message_draft` and shows draft/failed note; payload spreads `mensaje` only when trimmed non-empty; cleared on create success; nothing launches/sends. Supporting: `campaign.dto.ts` (`CampaignDto.mensaje?`, `AudienceBuildResultDto.message_draft?`), `campaign-repository.ts` + `use-campaign-mutations.ts` accept optional `mensaje`. `pnpm lint` PASS (0 errors / 4 pre-existing warnings); `npx tsc --noEmit` PASS exit 0.
- [x] 3.2 Add `ui.campaigns` copy keys in `lib/copy/ui.ts` (+ `en` mirror): message label/placeholder, AI draft hint, error. Verify: `pnpm lint`; keys referenced from the component. — DONE: `ui.campaigns` + `en.campaigns` inserted via small anchor-based edit right after the `crm` block in both es/en (coordination with PaletteFixer/InboxAssistFixer; no full-file rewrite): `messageLabel`, `messagePlaceholder`, `aiDraftHint`, `messageError`; all four referenced from `campaign-manager.tsx`; `lib/copy/ui.test.ts` (parity/non-empty sweeps) PASS.
- [x] 3.3 Component tests: AI build result pre-fills the textarea; user edits are preserved; create payload carries `mensaje`; create without message works; no send is triggered by creating a draft. Verify: `pnpm exec vitest run components/crm/campaign-manager.test.tsx` — passes. — DONE: fixtures gain `message_draft`; hoisted `mockCreateCampaignMutate`/`mockLaunchCampaignMutate`; `mockSegments` fixture so the create form can select a segment; 4 new tests added (prefill+hint, edits preserved → payload, create-without-mensaje → no `mensaje` key, no launch on draft create); vitest PASS (6 tests in campaign-manager.test.tsx).

## 4. Verification gate [OPS-GOV]

- [x] 4.1 Run backend gate: `sqlc generate` (docker cli) && `go build ./... && go vet ./... && go test ./internal/modules/campaigns/...`. Verify: all exit 0; record results here. — DONE: `sqlc generate` PASS (via `docker compose -f deps/docker-compose.yml run --no-deps --rm -w /workspace/internal/db/postgres/sqlc cli sqlc generate`; the note's plain `docker compose run --no-deps cli ...` fails with "no such service: cli" since the cli service lives in `deps/docker-compose.yml`); `go build ./...` PASS exit 0; `go vet ./...` PASS exit 0; `go test ./internal/modules/campaigns/...` PASS exit 0 (services, domain green; campaigns module has no test files in cmd/infra).
- [x] 4.2 Run frontend gate: `pnpm lint` (0 errors, pre-existing warnings acceptable) and `npx tsc --noEmit`. Verify: both pass; record results here. — DONE: `pnpm lint` PASS — 0 errors, 4 warnings (exactly the pre-existing baseline: command-palette react-hooks/incompatible-library, deal-kanban exhaustive-deps, prose no-img-element, +1 baseline); `npx tsc --noEmit` PASS exit 0.
- [x] 4.3 Run affected component tests: `pnpm exec vitest run components/crm/campaign-manager.test.tsx`. Verify: passes; record results. — DONE: `pnpm exec vitest run components/crm/campaign-manager.test.tsx lib/copy/ui.test.ts` — 2 files, 20 tests, all PASS (campaign-manager 6/6, ui copy 14/14).
- [x] 4.4 Record results and archive decision (`/opsx-archive` or `**Archive deferred:** <reason>`) in this file. Verify: entry present. — **Archive deferred:** all gates green (sqlc/build/vet/campaign tests exit 0, lint 0 errors / 4 baseline warnings, tsc clean, vitest 20/20, `pnpm build` PASS exit 0 — no lock contention), but archiving is deferred to the centralized Phase 5 fold so the archive step can preserve the repaired `## Requirements` header in `openspec/specs/whatsapp-campaigns/spec.md` (per the living-spec repair note above) and avoid racing the other in-flight Wave A/B changes (billing, tickets, inbox, palette, session, campaign send-path). Migration 000035 is additive (nullable column) and rollback is a drop — safe to land alongside peers.

## Note (2026-08-11, Phase 0): living-spec defect repair

Repaired `openspec/specs/whatsapp-campaigns/spec.md` — the `dabc95f` archive fold dropped the `## Requirements` wrapper header, breaking `openspec validate --specs --strict` (CI spec-validation gate). Fixed by inserting the header (one-line, no content change). This change owns the whatsapp-campaigns delta; the archive fold MUST preserve the `## Requirements` header.
