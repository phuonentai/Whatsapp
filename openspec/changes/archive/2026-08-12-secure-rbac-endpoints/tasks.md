# Tasks: secure-rbac-endpoints

## 1. Backend: secure the /rbac group [BE-INFRA]

- [x] 1.1 Add `resolver.Get("auth")` middleware to the `/rbac` group in `go-b2b-starter/internal/modules/auth/routes.go` (group-level `rbacGroup.Use(...)`, per design D1) and update the "public" comment at lines 22-23. No handler, DTO, or route changes. Verify: `go build ./...`; `grep -n "rbacGroup.Use" internal/modules/auth/routes.go` shows the auth middleware on the group. — DONE: `rbacGroup.Use(resolver.Get("auth"))` at routes.go:26; comment rewritten; `go build ./...` exit 0.
- [x] 1.2 Add a table-driven unit test file `routes_test.go` in the auth module (none exists today — only `middleware_test.go`) covering **all six** /rbac endpoints (`GET /roles`, `GET /permissions`, `GET /permissions/by-category`, `GET /roles/:role_id`, `POST /check-permission`, `GET /metadata`): unauthenticated → 401; authenticated (mock session) → 200 with unchanged JSON shape. Stub the RBACService the handlers call (`h.service`) for the authenticated case. Verify: `go test ./internal/modules/auth/...` — new table tests pass, existing auth tests green. — DONE: `routes_test.go` (stub MiddlewareResolver + real `RequireAuth`, mock-auth path for authenticated case, `NewRBACService()` stub service, envelope-aware shape assertions); `go test ./internal/modules/auth/...` green, `go vet` clean.

## 2. Frontend: remove the skipAuth bypass [FE-NEXT]

- [x] 2.1 Drop `skipAuth: true` from `getRoles()` in `next_b2b_starter/lib/api/api/repositories/rbac-repository.ts:39-41` so the request attaches the session token (client behavior per `api-client.ts:138`) and 401s hit the standard `handleUnauthorizedResponse` flow. Verify: `npx tsc --noEmit`; `grep -n "skipAuth" lib/api/api/repositories/rbac-repository.ts` returns no matches. — DONE: option removed; grep returns no matches.
- [x] 2.2 Add `rbac-repository.test.ts` defending the no-bypass contract (pattern: `signup-repository.test.ts` / `crm-repository.test.ts` mock `apiClient` + `resolveAccessToken`): `getRoles()` calls `apiClient.get("/rbac/roles", ...)` WITHOUT `skipAuth`; a 401 response routes through `handleUnauthorizedResponse`. Verify: `pnpm exec vitest run lib/api/api/repositories/rbac-repository.test.ts` — passes. — DONE: 2/2 tests pass (session-attached fetch asserted via call args; in-memory cache behavior). Note: the 401→`handleUnauthorizedResponse` routing lives in `api-client.ts` (mocked at this layer); the live 401 path is covered by the 3.2 e2e cases.

## 3. Verification gate [OPS-GOV]

- [x] 3.1 Run backend gate: `go build ./... && go vet ./... && go test ./internal/modules/auth/...`. Verify: all exit 0; record results here. — DONE (2026-08-11): `go build ./...` exit 0; `go vet ./...` exit 0; `go test ./internal/modules/auth/...` ok (incl. new `TestRBACRoutes_RequireAuth` table: 401×6 unauth, 200×6 + shape auth). Toolchain note: `go` not on default PATH — ran with `PATH=$PATH:/usr/local/go/bin` (go1.25.0).
- [x] 3.2 Add e2e mock-auth guard cases to `next_b2b_starter/e2e/specs/surrounding-processes.spec.ts` (pattern at :126): `GET /api/rbac/roles` → 401 without `X-Test-Org-ID`, 200 with it. Verify: `pnpm exec playwright test --config e2e/playwright.config.ts e2e/specs/surrounding-processes.spec.ts` — new cases pass, existing cases green. Optional: curl the same assertions on a dev stack and record status codes. — DONE: 2 new cases added next to the existing mock-auth guard; canonical stack booted (fresh `saas_db_test` + migrations 1-34 via migrate image with host networking + `cmd/seed-e2e` 5 orgs + backend :8080 `AUTH_MOCK_ENABLED=true`); spec run **7/7 passed** (5 existing + 2 new). Live curl on the same stack: unauth `GET /api/rbac/roles` → **401**, unauth `/api/rbac/metadata` → **401**, `X-Test-Org-ID` header → **200** with unchanged roles JSON.
- [x] 3.3 Run `pnpm lint` and `npx tsc --noEmit` in `next_b2b_starter`. Verify: lint exits 0 (3 pre-existing warnings in `components/crm/*` acceptable); tsc clean. — DONE: `pnpm lint` exit 0 — 0 errors, 4 warnings (all pre-existing: `components/crm/contact-table.tsx` ×2 incompatible-library, `components/crm/deal-kanban.tsx` exhaustive-deps, `components/marketing/prose.tsx` no-img-element); `npx tsc --noEmit` clean (incl. new `rbac-repository.test.ts` after adding `beforeEach` to the vitest import).
- [x] 3.4 Record results and archive decision (`/opsx-archive` or `**Archive deferred:** <reason>`) in this file. Verify: entry present. — **Archive deferred (2026-08-11):** all 8 tasks complete and locally verified — backend gate (`go build`/`go vet ./...`/`go test ./internal/modules/auth/...`) green, frontend lint (0 errors) + tsc clean, targeted e2e 7/7, live curl 401/200/shape confirmed. Archiving is deferred to the centralized verification phase per repo practice: full `make test-e2e` and full `pnpm test`/`pnpm build` run centrally (see `post-connect-whatsapp-onboarding` precedent), and the working tree carries other in-flight changes (311 modified files) — folding this delta into `openspec/specs/stytch-authorization` now would mix unrelated WIP. Rollback remains a Git-only revert of `routes.go`, `rbac-repository.ts`, `routes_test.go`, `rbac-repository.test.ts`, `surrounding-processes.spec.ts`, and this change's artifacts.
