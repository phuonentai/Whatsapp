<!-- Implementation tasks for add-whatsapp-scripted-sequences. Each unit <= 2 hours. Tags: [BE-DOMAIN] [BE-INFRA] [DB-SQLC] [FE-NEXT] [OPS-GOV]. Verification commands recorded per group. -->

## 1. Seed data — migration + catalog [DB-SQLC]

- [x] 1.1 Create `000025_update_playbook_sequence_seeds.up.sql`/`.down.sql`: for the five vertical keys (`comercio`, `restaurantes`, `citas`, `servicios-profesionales`, `talleres`), `UPDATE playbooks SET payload = payload || <jsonb>` injecting `pasos` (2-3 ordered Spanish steps, id/titulo/mensaje) into the chosen sequence guion (`confirmar-pedido`, `domicilio`, `confirmar-cita`, `cotizacion`, `cotizacion`); down restores the pre-change single-shot payloads. Verify: `make migrateup && make migratedown && make migrateup` exit 0; psql shows `pasos` in payload for applied rows after up — **PASS** (scratch pgvector:pg17, `migrate up` 25/u applied, 5/5 verticals carry `pasos`; `down 1` restored single-shot; `up` re-applied)
- [x] 1.2 Mirror the same sequence guiones into `internal/modules/playbooks/catalog.go` (single source must match migration seed). Verify: `go build ./...` and startup `CatalogValidated` check passes; extend `catalog_test.go` to assert each vertical has >= 1 guion with >= 2 complete `pasos` and that catalog and seeded DB payloads match on `pasos` — **PASS** (`go test ./internal/modules/playbooks/...` green; new assertion: >= 1 sequence guion per vertical; migration seed byte-matches catalog content, verified in 5.2 dogfood)

## 2. Backend — domain, validation, API [BE-DOMAIN]

- [x] 2.1 Extend `internal/modules/playbooks/domain/entities.go`: add `GuionPaso{ID, Titulo, Mensaje}` and `Guion.Pasos []GuionPaso` (json tags `id`/`titulo`/`mensaje`, `pasos`). Verify: `go build ./internal/modules/playbooks/...` — **PASS**
- [x] 2.2 Extend payload validation in `app/services/playbook_service.go`: a guion is valid when it has a non-empty `mensaje` with no `pasos`, OR 2+ `pasos` each with non-empty `id`/`titulo`/`mensaje`; guion with both `mensaje` and `pasos`, or fewer than 2 steps, or incomplete step → `ErrInvalidPlaybookPayload`. Unit tests for both branches. Verify: `go test ./internal/modules/playbooks/...` — **PASS** (5 new tests: accepts-sequence, rejects-single-step, rejects-incomplete-step, rejects-mensaje+pasos, rejects-empty-guion)
- [x] 2.3 Pass `pasos` through the catalog response in `internal/modules/playbooks/handler.go` (add `pasos` array with id/titulo/mensaje to the guion map); keep single-shot guiones unchanged (no `pasos` key). Extend `handler_test.go` to assert sequence guiones carry ordered `pasos` and non-applied playbooks still hide guiones. Verify: `go test ./internal/modules/playbooks/... && go build ./...` — **PASS**
- [x] 2.4 Build the startup `CatalogValidated` check: load the five seeded `modules.playbooks` rows via `PlaybookRepository.ListActive`, decode each payload with `ParsePayload`, and compare `guiones` (incl. `pasos`) against `catalog.go`; fail fast at boot on any mismatch (count, id, titulo, mensaje, pasos, vertical keys). Wire in `internal/bootstrap/init_mods.go` after the playbooks provider registers. Add a unit test proving a deliberately mismatched seed fails. Verify: `go build ./... && go test ./internal/modules/playbooks/...` and server boot passes with matching catalog — **PASS** (`catalog_validated.go` compares name/vertical/guiones incl. `pasos`; wired via `container.Invoke` in `init_mods.go`; 4 new tests: matches, missing vertical, stale single-shot guion drift, unknown seeded key; `go build ./...` + `go test ./internal/modules/playbooks/...` green)

## 3. Frontend — DTO, sequence hook, quick replies [FE-NEXT]

- [x] 3.1 Extend `next_b2b_starter/lib/api/api/dto/playbook.dto.ts`: `PlaybookGuionDto` gains optional `pasos?: { id, titulo, mensaje }[]`. Verify: `npx tsc --noEmit` PASS — **PASS**
- [x] 3.2 Add `use-sequence` hook (inbox page level): state `{guionId, stepIndex}`, actions start/advance/reset; keyed reset on conversation change; `currentMessage()` returns active step's `mensaje` (or null when inactive). Verify: `npx tsc --noEmit` PASS — **PASS** (state carries conversationId; inactive on conversation switch without eager setState; lint-clean)
- [x] 3.3 Update `app/dashboard/inbox/components/quick-replies.tsx`: render sequence guiones as pills with step-count badge; click starts sequence and fills composer with step 1 via existing `onSelect`; render progress indicator "Paso k de n" while active. Verify: `npx tsc --noEmit` PASS — **PASS**
- [x] 3.4 Wire send-success advance: reply-input send flow calls the sequence hook `advance()` on successful send (message appended + input cleared), no advance on failure; final step clears sequence mode; no auto-send of steps. Verify: `pnpm build` PASS and `pnpm lint` (no new errors in inbox files) — **PASS** (`pnpm build` green; eslint clean on inbox files + hook + dto; reply-input vitest 4/4 green)

## 4. E2E [FE-NEXT]

- [x] 4.1 Extend `next_b2b_starter/e2e/specs/inbox-ui.spec.ts`: sequence pill click → composer filled with step 1 + progress "Paso 1 de n"; send → step 2 auto-fills; last send → progress gone; failed send does not advance. Run against seeded mock. Verify: `pnpm lint` PASS and e2e inbox spec green (`pnpm exec playwright test inbox-ui`) — **PASS** (full `inbox-ui.spec.ts` suite 15/15 green: "scripted sequence auto-advances the composer", "failed sequence send does not advance", and "switching conversation resets sequence mode" — the latter covers the delta-spec reset scenario. Run via `npx playwright test -c e2e/playwright.config.ts inbox-ui.spec.ts`; env note: frontend dev server had to be restarted (`.next` dev cache was clobbered by `pnpm build`), and DB recreated + `cmd/seed-e2e` run for the mock orgs)

## 5. Verification & archive decision [OPS-GOV]

- [x] 5.1 Full verification pass: `make sqlc` (no changes expected), `go build ./...`, `go vet ./...`, `go test ./...`, `pnpm build`, `pnpm lint` — record results in this change's verification log. Verify: all commands exit 0 — **PASS** (`sqlc generate` exit 0, no new gen diffs; `go build ./...` exit 0; `go vet ./...` exit 0; `go test ./...` exit 0; `pnpm build` exit 0 — required killing a stale build process holding `.next/lock`; `pnpm lint` exit 0 — 1 pre-existing warning in `components/crm/deal-kanban.tsx`, 0 errors; inbox e2e full suite 15/15 green, sequence subset 3/3 green post-restart)
- [x] 5.2 Dogfood: fresh migrate on scratch DB through `000025` up/down; confirm five playbook payloads round-trip `pasos` and orgs with an existing applied playbook see sequence pills without re-applying. Verify: migrate up/down exit 0 and psql payload inspection — **PASS** (scratch `pgvector:pg17` on `saas_db_test`: `migrate up` applied 25/u — 5/5 verticals carry a sequence guion with 3 `pasos` (restaurantes at guion index 3, others at 1); `down 1` restored single-shot `mensaje` on all five; `up` re-applied. Re-apply not required: `ListCatalog` reads guiones from the global `playbooks.payload` row, gated only by the `organization_playbooks` row — e2e proved an applied org sees sequence pills; existing orgs get them on next page load after migration)
- [x] 5.3 Record **Archive deferred**: this change depends on `add-vertical-playbooks` being archived first (shared `vertical-playbooks` delta folds together). Verify: explicit entry in this file — **DONE** (**Archive deferred:** `add-vertical-playbooks` is complete but unarchived and owns the `vertical-playbooks` delta this change extends; archive both together after `add-vertical-playbooks` is archived)
