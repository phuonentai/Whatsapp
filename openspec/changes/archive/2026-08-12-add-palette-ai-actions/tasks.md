# Tasks: add-palette-ai-actions

## 1. Frontend: AI registry + palette group [FE-NEXT]

- [x] 1.1 Add `AiAction` type and `aiActionRegistry` to `lib/command-registry.ts` (parallel to `commandRegistry`, design D1) with three entries: Preguntar al asistente (`/dashboard/knowledge`), Nueva conversación de IA (`/dashboard/knowledge` + new-chat signal), Audiencia IA para campañas (`/dashboard/crm?view=campanas`); Spanish titles + keywords. Verify: `npx tsc --noEmit`.
  - Gate 2026-08-11: `npx tsc --noEmit` exit 0. `AiAction {id, title, section: "IA", icon, keywords?, onSelect(router)}` + `AiActionRouter` (structural `{push(href)}`, compatible with `AppRouterInstance` which is NOT re-exported by `next/navigation` in Next 16). Titles come from `ui.palette.*` (copy single source), keywords Spanish-first (+en mirror keywords inline). `ai-new-chat` onSelect calls `useCommandPaletteStore.getState().requestNewAiChat()` then pushes. Parallel registry — `commandRegistry`/`globalShortcuts` untouched.
- [x] 1.2 Add `aiNewChatSignal: number` + `requestNewAiChat()` to `lib/stores/command-palette-store.ts` (monotonic counter, design D3). Verify: `npx tsc --noEmit`.
  - Gate 2026-08-11: `npx tsc --noEmit` exit 0. Zustand state `aiNewChatSignal: 0` + `requestNewAiChat: () => set(s => ({ aiNewChatSignal: s.aiNewChatSignal + 1 }))`.
- [x] 1.3 Render an "IA" `CommandGroup` above the navigation sections in `components/common/command-palette.tsx` (filtered by the same query, executing `onSelect` then closing the palette, design D2/D4). Navigation + search mode untouched. Verify: `pnpm lint`; `npx tsc --noEmit`.
  - Gate 2026-08-11: `pnpm lint` exit 0 (0 errors, 4 pre-existing warnings); `npx tsc --noEmit` exit 0. `aiFiltered` memo mirrors `filteredDestinations`; group heading `ui.palette.iaGroup`; onSelect runs `action.onSelect(router)` then `onClose()` (closePalette). `PaletteBody` gained `router` + `onClose` props. Also: `promoteToSearch` now only promotes when BOTH nav and AI lists are empty — otherwise "audiencia" (AI-only match) would flip to contact search and hide the IA group.
- [x] 1.4 Subscribe `app/dashboard/knowledge/components/knowledge-content.tsx` to `aiNewChatSignal` (skip initial value) → existing `handleNewChat` (design D3). Verify: `pnpm lint`; `npx tsc --noEmit`.
  - Gate 2026-08-11: `pnpm lint` exit 0; `npx tsc --noEmit` exit 0. `handleNewChat` wrapped in `useCallback`; effect skips signal 0 and defers the reset one tick (`setTimeout 0`) — the React Compiler `react-hooks/set-state-in-effect` rule (error-level here) rejects synchronous setState in the effect body; async replay keeps the replay-on-mount semantics.
- [x] 1.5 Add `ui.palette` copy keys in `lib/copy/ui.ts` (+ `en` mirror): `iaGroup`, `askAssistant`, `newAiChat`, `aiCampaignAudience`. Verify: `pnpm lint`; keys referenced from the components.
  - Gate 2026-08-11: `pnpm lint` exit 0. Keys referenced: `askAssistant`/`newAiChat`/`aiCampaignAudience` as titles in `lib/command-registry.ts`; `iaGroup` as the group heading in `components/common/command-palette.tsx`. `en` mirror added. Namespace added anchor-only (concurrent editors: InboxAssistFixer `ui.agent`, CampaignMessageFixer `ui.campaigns` — no overlap; verified keys intact after their writes).
- [x] 1.6 Tests: extend `command-palette.test.tsx` — typing "asistente" shows the IA group; selecting "Preguntar al asistente" navigates to `/dashboard/knowledge`; selecting "Nueva conversación de IA" navigates AND increments the signal; selecting the audience action navigates to `/dashboard/crm?view=campanas`; existing navigation commands still pass. Add a knowledge bridge test: signal increment triggers `handleNewChat` (fresh chat state). Verify: `pnpm exec vitest run components/common/command-palette.test.tsx` — passes, existing tests green.
  - Gate 2026-08-11: `pnpm exec vitest run components/common/command-palette.test.tsx` → 11/11 passed (5 existing + 6 new). New tests: "shows the IA group when typing a matching AI keyword" ("asistente"), "navigates to the knowledge page when selecting 'Preguntar al asistente'" ("preguntar"), "navigates AND increments the new-chat signal when selecting 'Nueva conversación de IA'" (asserts `aiNewChatSignal === 1`), "navigates to the CRM campaigns view when selecting the audience action" ("audiencia"), "keeps existing navigation commands working alongside the IA group" ("knowledge" shows both IA + Knowledge Base), "resets the knowledge chat when the new-AI-chat signal increments" (bridge: select session → title in header; `requestNewAiChat()` inside `act` → header returns to "New Chat", session title left only in sidebar). Cognitive hooks mocked with STABLE references (render-phase `prevSessions` setState would loop otherwise — fixed after AuditFixer reported "Too many re-renders").

## 2. Verification gate [OPS-GOV]

- [x] 2.1 Run frontend gate: `pnpm lint` (0 errors, pre-existing warnings acceptable) and `npx tsc --noEmit`. Verify: both pass; record results here.
  - Gate 2026-08-11: `pnpm lint` exit 0 — 0 errors, 4 pre-existing warnings (TanStack Virtual incompatible-library ×2, exhaustive-deps deal-kanban, no-img-element prose). `npx tsc --noEmit` exit 0.
- [x] 2.2 Run affected component tests: `pnpm exec vitest run components/common/command-palette.test.tsx`. Verify: passes; record results.
  - Gate 2026-08-11: `pnpm exec vitest run components/common/command-palette.test.tsx` exit 0 — Test Files 1 passed, Tests 11 passed (11/11). Only remaining stderr noise is the pre-existing `act()` warning from the `openPalette()` store helper used by all tests (unrelated to this change).
- [x] 2.3 Record results and archive decision (`/opsx-archive` or `**Archive deferred:** <reason>`) in this file. Verify: entry present.
  - Gate 2026-08-11: recorded above. **Archive deferred:** all gates green (lint 0 errors, tsc clean, command-palette vitest 11/11, knowledge bridge covered) — archiving deferred until the orchestrator's commit pass while concurrent changes (auth-audit-events, add-inbox-ai-writing-assist, add-campaign-ai-message, fix-subscription-status-trialing) are in flight; no blockers from this change. `pnpm build` intentionally NOT run (shared `.next` dir; not a gate for this change).
