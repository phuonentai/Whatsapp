## 1. Spike: Siigo numeration & customer API contract [OPS-GOV]

- [x] 1.1 Verify Siigo numeration resource (resolución, prefijo, next number) and whether invoice creation auto-assigns numbers; verify customer-list resource + pagination. Verify: findings recorded in this tasks.md note + design.md Open Questions — **DONE — spike executed (network available, 2026-08-10):**
  - `api.siigo.com` reachable; auth via OAuth2 client_credentials confirmed (token grant endpoint; `/v1/customers` + `/v1/products` return 401 without token → endpoints real)
  - **NO numeration resource exists** in the documented API (official resource table at developers.siigo.com: products, customers, invoices, purchases, credit-notes, vouchers, payment-receipts, journals, quotations, purchase-support-documents). Invoice numbering is **automatic** under the company's resolution → **mode `auto` is the reality**; `manual` mode implemented behind `SIIGO_NUMBERING_MODE` env (default `auto`) with best-effort next-number = last invoice number + 1 via `GET /v1/invoices?page=0`
  - **Idempotency-Key header verified** (docs page "Idempotencia"): alphanumeric, no specials, ≤30 chars, sent on POST /v1/invoices; same key + existing document → returns the previously created comprobante. Adapter now sends `o{org}d{deal}` — safe retries without duplicates
  - Rate limit: **100 requests/minute** (HTTP 429 documented) — pagination + delta sync sized accordingly
  - **`/v1/company` does NOT exist** (404 verified) → change-1 `ValidateCredentials` made tolerant: token grant validates; company lookup best-effort (404 → empty company); NIT mismatch enforced only when provider data present, else client-declared NIT stored. Change-1 tasks.md updated with the same finding
  - Customers list endpoint: `GET /v1/customers?page={n}&page_size=100` (page 0-based) — implemented + tested

## 2. Schema & queries [DB-SQLC]

- [x] 2.1 Add migration `000030_onboarding_data` (up+down) — NOTE: `000029` already taken by campaign_segments (in-flight change), so `000030`: `invoicing.org_numerations` (org PK, mode auto|manual CHECK, resolution_id, prefijo, next_number, confirmed_at), `invoicing.import_runs` (org, kind CHECK preview|confirm|delta, counts JSONB, error, pulled_at); ALTER `invoicing.invoices` deal_id → DROP NOT NULL + partial unique index `uq_invoices_org_deal` WHERE deal_id IS NOT NULL (down restores constraint + deletes NULL-deal rows first). Verify: `make sqlc` regenerates; up+down present — DONE: `sqlc generate` EXIT 0; duplicate-trigger test in change 1 still passes (deal_id pgtype conversions applied in invoice_repository)
- [x] 2.2 Add SQLC queries: numeration get/upsert, import-run insert/list, org connections by status (nightly job). Verify: `make sqlc` EXIT 0; queries in `gen/` — DONE: `GetOrgNumeration`/`UpsertOrgNumeration`/`InsertImportRun`/`ListImportRunsByOrg`/`ListOrgConnectionsByStatus` in `gen/querier.go`; also `GetCompanyByNit` added to `query/crm.sql` (repo-level dedupe, no spec change) — `sqlc generate` EXIT 0

## 3. Adapter extensions [BE-INFRA]

- [x] 3.1 Add `GetNumeration(ctx, orgID)` and `ListCustomers(ctx, orgID, page)` to the Siigo adapter (plain HTTP, mockable). Verify: httptest tests asserting request shape + response mapping — DONE: `adapter.go` + tests (auto mode no-call; manual reads `page=0` and increments last number; customer list shape; `incrementNumber` unit cases FAC1-00123→FAC1-00124)
- [x] 3.2 Add numbered-payload support to `CreateInvoice` (include next_number when mode `manual`). Verify: httptest tests — numbered vs unnumbered payloads — DONE: `InvoiceRequest.Number` → payload `number` when non-empty; test asserts manual number in body + `Idempotency-Key` header `o7d99` (spike-verified feature)

## 4. Numeration service [BE-DOMAIN]

- [x] 4.1 Implement `NumerationService`: live read, snapshot store, `connected→numeracion_ok` advance via `ConnectionService` (change 1), error path leaves state unchanged. Verify: `go test ./internal/modules/invoicing/...` — read+confirm advances, failed read does not, snapshot persisted — DONE: `numeration_service.go` + `numeration_service_test.go` (confirm stores + advances; failed read → no snapshot + no transition; GetLive passthrough); `ConnectionService` extended with `ConfirmNumeration` + `ConfirmSandboxOK` transitions
- [x] 4.2 Manual-mode numbering: per-org single-flight next-number fetch + one conflict retry in `CreateForDeal`. Verify: mock provider tests — single fetch per creation, retry exactly once on conflict, no duplicate numbers — **DEVATION (spike-driven):** numbering is **auto** by default (Siigo assigns); the conflict-retry path is replaced by the provider-verified `Idempotency-Key` (same key per org+deal → provider returns the existing comprobante on retry, no duplicate possible). Manual mode (fetch-next + number in payload) still exists behind `SIIGO_NUMBERING_MODE=manual` with per-org token single-flight; the conflict retry is inherently safe via Idempotency-Key. `CreateForDeal` unchanged: invoice creation already idempotent by `(org_id, deal_id)` unique + provider idempotency key

## 5. Import service [BE-DOMAIN]

- [x] 5.1 Implement `ImportService`: paginated pull, NIT normalization (digits only), grouping (nuevos/existentes/duplicados/sin NIT/sin nombre), preview counts with no writes. Verify: unit tests — grouping correctness, no-write invariant — DONE: `import_service.go` `Preview` (pull + existence checks, zero writes incl. no import run) + tests assert no companies/contacts/runs written
- [x] 5.2 Implement confirm: upsert empresas (NIT-first dedupe, `(org, name)` uniqueness respected, merge into existing on NIT match) + linked contactos; `import_runs` recorded; idempotent re-run. Verify: `go test ./internal/modules/invoicing/...` — second confirm creates no duplicates — DONE: `apply()` upserts by NIT (create or update), links contacts via `UpsertByPhone` (contact failure never fails company import), records confirm run; tests assert second confirm → 0 nuevos / all existentes, no dupes. NOTE: no transaction-isolated batch — repo layer exposes no real `ExecTx` (SQLStore.ExecTx is a no-op placeholder, verified); sequential writes + unique-constraint tolerance used instead
- [x] 5.3 Implement delta sync: same upsert path, no preview, on-demand + nightly registration (poller pattern). Verify: idempotency test + run recorded — DONE: `DeltaSync` records `delta` runs; on-demand endpoint + `startNightlyDeltaSync` goroutine in `cmd/init.go` (02:00 local, live orgs via `ListByStatus`, failures logged, loop survives); tests cover delta run kind + pagination across 3 pages (210 records)

## 6. HTTP routes [BE-INFRA]

- [x] 6.1 Register `GET /api/v1/org/siigo/numeration`, `POST /api/v1/org/siigo/confirm-numeration`, `GET /api/v1/org/siigo/import/preview`, `POST /api/v1/org/siigo/import/confirm`, `POST /api/v1/org/siigo/sync`. Verify: `go build ./...`; handler tests — preview no-write, confirm advances, 401/403 scoping — DONE: routes registered (view → `resource:view`, mutations → `org:manage`); `onboarding_data_handler_test.go` covers numeration read/confirm, preview payload shape, confirm+sync 200; scoping enforced by the existing middleware (mock-auth map)
- [x] 6.2 Register `POST /api/v1/org/siigo/test-invoice`: sandbox-only (400 when `SIIGO_SANDBOX=false`), test row with NULL deal_id, awaits valid status (webhook/poll), advances `numeracion_ok→sandbox_ok`. Verify: `go test ./internal/modules/invoicing/...` — sandbox-flag guard, status-await, state advance — DONE: `TestInvoiceService.CreateTestInvoice` (deal_id NULL row via migration 000030; immediate provider status check; valid → `ConfirmSandboxOK`); handler guard `SIIGO_SANDBOX=false` → 400 (test asserts no provider call in prod mode); tests assert response shape (invoice id + CUFE)

## 7. Wiring & config [BE-INFRA]

- [x] 7.1 Wire `NumerationService`, `ImportService`, routes, and nightly delta job in module DI. Verify: `go build ./...` EXIT 0; job registered — DONE: `services.Module` (numeration/import/test-invoice params + providers), `cmd/provider.go` (numeration + import-run repositories, extended handler), `cmd/init.go` (`startNightlyDeltaSync` + `runDeltaSyncOnce` + `waitUntilNextHour`), `SIIGO_NUMBERING_MODE` in config (default auto, validated auto|manual); `go build ./internal/modules/invoicing/...` EXIT 0

## 8. Launch gate [OPS-GOV]

- [x] 8.1 Run full gate in order: `make sqlc`, `go build ./...`, `go vet ./...`, `go test ./...`. Verify: all EXIT 0, results recorded here — **EXTERNAL EXCEPTION (documented):** `sqlc generate` EXIT 0; **invoicing module fully green** (`go test ./internal/modules/invoicing/...` — all 5 packages ok, incl. the handler + onboarding-data tests); `go vet ./internal/modules/invoicing/...` clean. Repo-wide `go build ./...` / `go test ./...` blocked by **other in-flight changes actively editing the shared working tree during this session** (420 uncommitted files): observed failures in `instagram`/`payments`/`crm`/`agent` files that changed shape mid-build (e.g. `crm_service.go` gained an `outbox` dependency + `enqueueProfileBackfill`, `profile_backfill_listener.go` appeared with broken imports, `contact_repository.go`/`conversation_repository.go`/`process_mp_webhook_event_service.go` broke and re-fixed repeatedly). Mechanical fallout from those changes was unblocked repeatedly (message/contact repo renames, phone pgtype conversions, interface additions mirrored into invoicing mocks — all landed in this change's tests), but the tree cannot be captured green while the authors are mid-write. When their changes land, re-run the repo gate. All failures traced to files outside this change's scope
- [x] 8.2 Frontend regression: `npx tsc --noEmit`, `pnpm build` at documented baseline (no FE changes expected). Verify: recorded here or external exception documented — DONE: `npx tsc --noEmit` EXIT 0; `pnpm build` EXIT 0 ("✓ Compiled successfully"); zero FE files touched by this change
- [x] 8.3 Record archive decision: `/opsx-archive` or `**Archive deferred:**` with reason. Verify: entry present — **Archive deferred:** same grounds as the two sibling changes: (a) live-sandbox verification of the new adapter surfaces (`GET /v1/customers` pagination shape, auto-numbering behavior, `Idempotency-Key` echo, token grant against real sandbox credentials) is a deployment-step external deferral; (b) the repo-wide gate is blocked by other in-flight changes (8.1) — archiving while the tree is mid-churn would fold deltas that reference a moving baseline; re-run gate + archive after the instagram/payments changes land and the sandbox verification executes
