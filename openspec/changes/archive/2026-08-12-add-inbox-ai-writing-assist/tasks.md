# Tasks: add-inbox-ai-writing-assist

## 1. Backend: rephrase endpoint [BE-DOMAIN]

- [x] 1.1 Add `RephraseText(ctx, orgID int32, text, mode string) (string, error)` to `internal/modules/agent/app/services/agent_service.go` following the `analyze()` pattern (design D2): credit gate via `billing.GetAiUsageStatus` (ledger error → warn + fail-open; `CreditsMax > 0 && remaining <= 0` → `ErrAICreditsExhausted`), `llmdomain.WithOrgID(ctx, orgID)` for metering, metered `s.llm.Complete` with `rephraseSystemPrompt(mode)`, trimmed text return. Define the sentinel error and `rephraseSystemPrompt` (modes: rephrase/formal/casual/summarize). Verify: `go build ./...`.
  - Gate 2026-08-11: `go build ./internal/modules/agent/...` exit 0; `go vet` clean; repo-wide `go build ./...` blocked only by CampaignMessageFixer's in-flight `internal/modules/campaigns/app/services/ai_audience_builder.go:94` (`undefined: parseAIBuildResponse`) — not my file, notified peer.
- [x] 1.2 Add handler `HandleRephrase` (bind `{text, mode}`; 400 `text_required` / `invalid_mode` before any LLM call; 200 envelope `{"data":{"text":...},"success":true}`; 402 `ai_credits_exhausted`; 500 wrapped) and register `POST /rephrase` in `internal/modules/agent/routes.go` with `auth` + `org_context` + `subscription` group middleware and `auth.RequirePermissionFunc("org", "view")`. Verify: `go build ./...`; `grep -n "rephrase" internal/modules/agent/routes.go`.
  - Gate 2026-08-11: `go build ./internal/modules/agent/...` exit 0; `grep -n "rephrase" internal/modules/agent/routes.go` → line 71 `group.POST("/rephrase", auth.RequirePermissionFunc("org", "view"), r.handler.HandleRephrase)` under the `auth`+`org_context`+`subscription` group.
- [x] 1.3 Unit tests (`agent_service_test.go` or new `rephrase_test.go` in the agent package): each mode returns transformed text (mock LLM); exhausted credits → error and NO LLM call; ledger failure → fail-open proceeds; invalid mode → error before LLM; metered recording asserted; route-level 401 unauth / 200 auth / 400 invalid mode. Verify: `go test ./internal/modules/agent/...` — all pass.
  - Gate 2026-08-11: `go test ./internal/modules/agent/...` → ok (agent, app/services, infra/guardrails). New files: `app/services/rephrase_test.go` (TestRephraseTextEachModeReturnsTransformedText / ExhaustedCreditsSkipsLLMCall / LedgerFailureFailsOpen / InvalidModeErrorsBeforeLLM / LLMFailureWrapsError) and `routes_test.go` (TestHandleRephraseUnauthenticatedReturns401 / SuccessEnvelope / InvalidModeReturns400 / CreditsExhaustedReturns402).

## 2. Frontend: composer writing assist [FE-NEXT]

- [x] 2.1 Add `rephrase(text, mode)` to `lib/api/api/repositories/agent-repository.ts` (+ DTO/model for the rephrase request/response, following the existing `Wrapped<T>` envelope) and a `useRephraseMutation` hook. Verify: `npx tsc --noEmit`.
  - Gate 2026-08-11: `npx tsc --noEmit` exit 0. Added `RephraseRequestDto`/`RephraseResponseDto` (agent.dto.ts), `RephraseMode`/`RephraseResponse` (agent.model.ts), `agentRepository.rephrase()` (Wrapped envelope), new `lib/hooks/mutations/use-rephrase-mutation.ts` (thin `useRephraseMutation` + `isAiCreditsExhausted` 402 guard).
- [x] 2.2 Add the Sparkles action dropdown to `app/dashboard/inbox/components/reply-input.tsx` (items Rephrase / Hacer formal / Hacer casual / Resumir), disabled without text/conversation or while in-flight; on success `setText(result.text)` (draft replaced for review, never auto-sent); on error keep draft and toast; loading state. Verify: `pnpm lint`; `npx tsc --noEmit`.
  - Gate 2026-08-11: `pnpm lint` 0 errors / 4 pre-existing warnings (baseline); `npx tsc --noEmit` exit 0. Sparkles trigger beside Send with 4 items; `assistDisabled = !text.trim() || !conversationId || isSending || isRephrasing`; success → `setText(result.text)`; failure → credits-exhausted or generic toast, draft kept; Loader2 spinner while in-flight; send button got `aria-label="Enviar"`.
- [x] 2.3 Add copy keys under `ui.agent` in `lib/copy/ui.ts` (+ `en` mirror): rephrase action labels, error, credits-exhausted message. Verify: `pnpm lint`; keys referenced from the component.
  - Gate 2026-08-11: added `rephraseTrigger`, `rephrase`, `rephraseFormal`, `rephraseCasual`, `rephraseSummarize`, `rephraseError`, `rephraseCreditsExhausted` to both `ui.agent` (es) and `en.agent` (mirror) via anchor edits (no clobber with concurrent palette/campaigns/tickets namespaces); all referenced from `reply-input.tsx`; `pnpm lint` 0 errors.
- [x] 2.4 Component tests (`reply-input.test.tsx`): clicking Rephrase calls the mutation and replaces the input value; failure keeps the draft text and shows a toast; in-flight state disables actions; no send is triggered. Verify: `pnpm exec vitest run app/dashboard/inbox/components/reply-input.test.tsx` — passes.
  - Gate 2026-08-11: `pnpm exec vitest run app/dashboard/inbox/components/reply-input.test.tsx` → Test Files 1 passed, Tests 9 passed (9/9): 5 pre-existing send-path tests (updated queries for the new aria-labels) + 4 new (mutation replaces input & no send; failure keeps draft + generic toast; 402 → credits-exhausted toast; disabled without text/conversation/in-flight). Full suite: 41 files / 197 tests passed.

## 3. Verification gate [OPS-GOV]

- [x] 3.1 Run backend gate: `go build ./... && go vet ./... && go test ./internal/modules/agent/...`. Verify: all exit 0; record results here.
  - Gate 2026-08-11: `go build ./internal/modules/agent/...` exit 0; `go vet ./internal/modules/agent/...` exit 0; `go test ./internal/modules/agent/...` → ok (agent, app/services, infra/guardrails). Repo-wide `go build ./...` currently fails ONLY on CampaignMessageFixer's uncommitted WIP `internal/modules/campaigns/app/services/ai_audience_builder.go:94` (`undefined: parseAIBuildResponse`) — peer-owned file, notified; all other packages build (checked via `go build $(go list ./... | grep -v /campaigns)` exit 0).
- [x] 3.2 Run frontend gate: `pnpm lint` (0 errors, pre-existing warnings acceptable) and `npx tsc --noEmit`. Verify: both pass; record results here.
  - Gate 2026-08-11: `pnpm lint` exit 0 — 0 errors / 4 warnings (documented baseline: react-hooks/incompatible-library ×2 contact-table + virtualizer, react-hooks/exhaustive-deps deal-kanban, @next/next/no-img-element prose.tsx); `npx tsc --noEmit` exit 0.
- [x] 3.3 Run affected component tests: `pnpm exec vitest run app/dashboard/inbox/components/reply-input.test.tsx`. Verify: passes; record results.
  - Gate 2026-08-11: passed — Test Files 1 passed, Tests 9 passed (9/9). Full suite also run: 41 files / 197 tests passed (no regressions from shared-file edits).
- [x] 3.4 Record results and archive decision (`/opsx-archive` or `**Archive deferred:** <reason>`) in this file. Verify: entry present.
  - **Archive deferred:** all change gates green — backend agent module `go build`/`go vet`/`go test ./internal/modules/agent/...` exit 0; FE `pnpm lint` 0 errors / 4 baseline warnings, `npx tsc --noEmit` exit 0, `reply-input.test.tsx` 9/9 (full vitest suite 41 files / 197 tests passed), `pnpm build` exit 0 (retried once after `.next` lock contention). Archiving deferred until the orchestrator's commit pass while concurrent changes are in flight: repo-wide `go build ./...` is currently blocked only by CampaignMessageFixer's uncommitted `internal/modules/campaigns/app/services/ai_audience_builder.go:94` (`undefined: parseAIBuildResponse`, peer notified); TicketTriageFixer/SessionHardeningFixer WIP also pending. No blockers from this change.
