## 1. Backend MFA Policy (verified done — unchanged)

- [x] 1.1 [BE-DOMAIN] Add `MfaPolicyUpdater` interface to `organizations/domain` (`UpdateMfaPolicy(ctx, orgID, policy, methods, allowedMethods)`) with strict types (`MfaPolicy = OPTIONAL | REQUIRED_FOR_ALL`, `MfaMethods = ALL_ALLOWED | RESTRICTED`, `MfaMethod = totp | sms_otp`). Verification: `make build`; `go test ./internal/modules/organizations/...` passes.
  - New `internal/modules/organizations/domain/mfa_policy.go`: `MfaPolicy`/`MfaMethods`/`MfaMethod` typed constants, `MfaPolicyUpdater` interface, `ValidateMfaPolicyUpdate` (policy/methods enums, non-empty allowlist when RESTRICTED, no duplicates). Domain errors added in `errors.go` (`ErrInvalidMfaPolicy`, `ErrInvalidMfaMethods`, `ErrInvalidMfaMethod`, `ErrMfaPolicyUnavailable`).
  - GATE: `export PATH=$PATH:/usr/local/go/bin && go build ./...` PASS; `go test ./internal/modules/organizations/...` PASS (2026-08-11).

- [x] 1.2 [BE-INFRA] Implement in the Stytch auth adapter: `organizations.update` with `mfa_policy`, `mfa_methods`, `allowed_mfa_methods`, wrapped in the existing two-tier circuit breaker; map breaker-open/5xx to 503 with structured error code. Verification: `make build`; adapter unit tests (success, Stytch 4xx, breaker-open → 503) pass.
  - `stytchOrganizationRepository.UpdateMfaPolicy` (implements `domain.MfaPolicyUpdater`): `PUT /v1/b2b/organizations/{org_id}` behind the shared `stytchcfg.Client.Run` breaker; 4xx surfaces as a normal error, breaker-open (`ErrCircuitOpen`) and 5xx (`ErrInternal`) map to `domain.ErrMfaPolicyUnavailable` (503 sentinel). `NewStytchOrganizationRepository` now returns the concrete type so one instance satisfies `AuthOrganizationRepository` + `MfaPolicyUpdater`; wired in `module.go` DI.
  - Adapter tests `stytch_organization_repository_test.go` (httptest + real SDK client): happy path (body asserts mfa_policy/mfa_methods/allowed_mfa_methods), 4xx surfaces (NOT 503), 5xx → `ErrMfaPolicyUnavailable`, breaker-open → `ErrMfaPolicyUnavailable` + API calls stop, validation (empty org, bad policy/methods/method, restricted-without-allowlist).
  - GATE: `go build ./...` PASS; adapter tests PASS; full `go test ./...` PASS (2026-08-11).

- [x] 1.3 [BE-INFRA] Expose `PUT /api/organizations/mfa-policy` (authenticated, `org:manage`) delegating to the service. Verification: `make build`; handler test with mock auth passes; `go test ./internal/modules/organizations/...` passes.
  - `OrganizationService.UpdateMfaPolicy(ctx, orgID, policy, methods, allowedMethods)` validates + delegates to `MfaPolicyUpdater` (new dep; service + interface + module.go + test fakes updated). Profile read path extended: `AuthOrganization`/`ProfileOrganization` carry `mfa_policy`/`mfa_methods`/`allowed_mfa_methods` from the Stytch org object (task 4.1 read).
  - Route registered in `internal/modules/organizations/routes.go` (`orgGroup.PUT("/mfa-policy")` with `auth.RequirePermissionFunc("org", "manage")`); new handler `mfa_policy_handler.go` maps `ErrMfaPolicyUnavailable`/`ErrAuthConnection` → 503 with structured code `mfa_policy_update_unavailable`, validation → 400.
  - Handler tests `mfa_policy_handler_test.go` (mock auth via X-Test-Org-ID + stub org_context): 200 success (delegates org-1/REQUIRED_FOR_ALL), 403 member identity without org:manage, 401 unauthenticated, 400 invalid policy payload, 503 structured error on `ErrMfaPolicyUnavailable`. Service tests: delegation args, validation rejection, updater error propagation.
  - GATE: `go build ./...` PASS; `go test ./internal/modules/organizations/...` PASS; full `go test ./...` PASS (2026-08-11).

## 2. MFA Rate Limiter (council #3 — new prerequisite)

- [x] 2.1 [FE-NEXT] Add `lib/auth/mfa-limiter.ts` reusing the magic-link limiter's in-process sliding-window pattern: per-member and per-IP windows, env-configurable (`MFA_RATE_LIMIT_PER_MEMBER_PER_HOUR` default 10, `MFA_RATE_LIMIT_PER_IP_PER_HOUR` default 30), single-instance assumption documented. Applied to `authenticateTotp` (esp. recovery-code path); limiter rejection returns a generic "try again later" error that does not reveal code validity. Unit tests: per-member/IP window enforcement. Verification: `pnpm test -- mfa-limiter` passes.
  - New `lib/auth/mfa-limiter.ts`: `createMfaLimiter` + `checkMfaRateLimit` singleton, per-member (`member:` key, `MFA_RATE_LIMIT_PER_MEMBER_PER_HOUR` default 10) + per-IP (`ip:` key, `MFA_RATE_LIMIT_PER_IP_PER_HOUR` default 30) sliding windows, env re-read per check, throttled attempts not recorded, single-instance assumption documented; per-IP window is the hard backstop when no member id is available.
  - GATE: `npx vitest run lib/auth/mfa-limiter.test.ts` PASS (9 tests: member/IP windows, sliding re-allows, key independence both directions, no-member-id IP backstop, env overrides, unparseable-env fallback) (2026-08-13).

## 3. MFA Server Actions

- [x] 3.1 [FE-NEXT] Add `createTotp` + `authenticateTotp` server actions (`lib/actions/auth/mfa.ts`): `createTotp(sessionJwt)` — duplicate guard: read member `totp_registration_id`; if a registration exists, return "manage existing" (no create call); else `totp.create` with `session_jwt`; `authenticateTotp({intermediateSessionToken, code, recoveryCode?})` — TOTP path via `totp.authenticate`; recovery path via `totp.recovery_codes.recover` which returns the session DIRECTLY (assert no chained TOTP authenticate); set session cookies on success only; best-effort `login_succeeded` / bounded `mfa_challenge_failed` audit; rate-limited (2.1). Verification: `pnpm lint`; `pnpm build` passes.
  - New `lib/actions/auth/mfa.ts`: `createTotp()` (session derived server-side from httpOnly cookies — the client can never supply the session JWT; duplicate guard via `totp_registration_id` before any create call; returns `created` with qr_code/secret/recovery_codes or `existing`), `verifyTotpEnrollment({code})` (totp.authenticate with `set_mfa_enrollment: enroll` on the member session — enrollment applies after verification), `authenticateTotp({intermediateSessionToken, code?, recoveryCode?, memberId?, organizationId?})` (TOTP path via totps.authenticate; recovery path via recoveryCodes.recover returning the session DIRECTLY — no chained authenticate; cookies set ONLY on success; mfa_challenge_passed + login_succeeded on success; bounded mfa_challenge_failed on failure/rate-limit; rate-limited via checkMfaRateLimit before any outbound call; generic error never reveals code validity).
  - GATE: `pnpm lint` PASS (0 errors / 4 pre-existing warnings); `pnpm build` PASS (2026-08-13).

- [x] 3.2 [FE-NEXT] Unit tests with mocked Stytch client: TOTP success sets cookies; wrong code sets no cookies; recovery success sets cookies from the recover response and issues NO follow-up authenticate call; rate-limit rejection returns generic error; createTotp with existing `totp_registration_id` performs no create call. Verification: `pnpm test -- mfa` passes.
  - New `lib/actions/auth/mfa.test.ts` (mocked getStytchB2BClient + getMemberSession + next/headers cookies/headers + recordAuthAudit): TOTP success sets session cookies + audits mfa_challenge_passed/login_succeeded; wrong code sets NO cookies + bounded mfa_challenge_failed; recovery success sets cookies from recover response with NO chained totps.authenticate call; rate-limit rejection (env 1/h member) returns generic error, only 1 Stytch call, bounded audit; createTotp with existing registration performs NO create call; createTotp without registration calls totps.create with session_jwt; verifyTotpEnrollment passes set_mfa_enrollment=enroll and sets no cookies.
  - GATE: `npx vitest run lib/actions/auth/mfa.test.ts` PASS (10 tests) (2026-08-13).

## 4. Login MFA Continuation (FE)

- [x] 4.1 [FE-NEXT] `app/authenticate/page.tsx`: when `memberAuthenticated === false && mfaRequired`, render TOTP challenge step (code input, recovery-code toggle, error states) instead of failing; on success redirect to destination. Verification: `pnpm build`; page tests cover wrong-code error and recovery path.
  - `app/authenticate/page.tsx` extended: after `consumeMagicLink` returns `memberAuthenticated: false && mfaRequired`, the page renders the MFA challenge step (TOTP code input, "use recovery code" toggle, generic error states) holding the intermediate session token + member/org ids (context only); on success redirects to the destination; on failure stays on the challenge step. Copy strings added to both ES/EN locale blocks in `lib/copy/ui.ts`.
  - New `app/authenticate/page.test.tsx` (mocked consumeMagicLink + authenticateTotp + next/navigation): challenge renders instead of failing; wrong code shows generic error and stays on step with no redirect; recovery-code sign-in completes in one exchange (authenticateTotp called with recoveryCode + intermediateSessionToken) and redirects.
  - GATE: `npx vitest run app/authenticate/page.test.tsx` PASS (3 tests); `pnpm build` PASS (2026-08-13).

- [x] 4.2 [FE-NEXT] Wire `mfa_challenge_passed`/`mfa_challenge_failed` audit events (per auth-audit-events taxonomy), with bounded detail on failed/rate-limited recovery attempts. Verification: `pnpm test` passes.
  - Wired in `lib/actions/auth/mfa.ts` (task 3.1): success records `mfa_challenge_passed` + `login_succeeded`; failure records `mfa_challenge_failed` with `mapAuthErrorToDetail` (bounded enum, unmapped → `internal_error`); rate-limited attempts record `mfa_challenge_failed` with bounded `internal_error` detail (never reveals code validity).
  - GATE: `pnpm test` PASS (full suite, 56 files / 292 tests) (2026-08-13).

## 5. Member Enrollment UI (Settings → Profile → Security)

- [x] 5.1 [FE-NEXT] Settings profile "Security" section: render the Stytch-provided `qr_code` image (NO QR library — server-generated) + manual secret + verify-code step + recovery-codes-once confirmation; surface existing registration (`totp_registration_id`) for management instead of creating a duplicate. Verification: `pnpm build`; component tests pass.
  - New `app/dashboard/settings/components/security-section.tsx` rendered in the profile view (settings-content case "profile"): createTotp → server-generated `qr_code` `<img>` + manual secret + verify step (verifyTotpEnrollment) → one-time recovery codes with "Guardé estos códigos" confirmation; existing `totp_registration_id` surfaced as manage-existing card (rotate recovery codes via new `rotateRecoveryCodes` action) — no duplicate create. No QR library; secrets/codes never persisted locally.
  - New `app/dashboard/settings/components/security-section.test.tsx` (mocked mfa actions): QR image + secret rendered from createTotp response; recovery codes shown once after verify; existing registration surfaces without a duplicate create call and supports rotation.
  - GATE: `npx vitest run app/dashboard/settings/components/security-section.test.tsx` PASS (3 tests); `pnpm build` PASS (2026-08-13).

## 6. Org Policy UI (FE)

- [x] 6.1 [FE-NEXT] Compliance/security section: read current policy from profile (display-only mirror — never used for authorization decisions); POST `PUT /api/organizations/mfa-policy`; gate by `org:manage`; show 503 structured error on backend failure. Verification: `pnpm build`; component tests pass.
  - New `app/dashboard/settings/components/mfa-policy-section.tsx` rendered in the compliance view (settings-content case "compliance", itself gated by org:manage): reads policy/methods/allowed methods from the profile mirror (display-only, documented invariant); `organizationRepository.updateMfaPolicy` → `PUT /api/organizations/mfa-policy`; 503 structured error surfaced distinctly.
  - Supporting: `ProfileOrganizationDto` + `UserProfile` + `member-repository.toUserProfile` extended with `mfa_policy`/`mfa_methods`/`allowed_mfa_methods` mirrors; `organization-repository.updateMfaPolicy` added.
  - New `app/dashboard/settings/components/mfa-policy-section.test.tsx`: mirror reflected as initial selection; save posts `{mfa_policy, mfa_methods, allowed_mfa_methods: ["totp"]}`; 503 error shown on backend failure.
  - GATE: `npx vitest run app/dashboard/settings/components/mfa-policy-section.test.tsx` PASS (3 tests); `pnpm build` PASS (2026-08-13).

## 7. Verification Gate

- [x] 7.1 [BE-INFRA] `make build`; `go test ./internal/modules/organizations/...` and adapter tests pass.
  - GATE: `go build ./...` PASS; `go test ./internal/modules/organizations/...` PASS (handler + service + repository packages); `go test ./internal/platform/stytch/...` PASS; targeted MFA tests (handler 200/403/401/400/503, service delegation/validation/error propagation, adapter happy-path/4xx/5xx/breaker-open) all PASS (2026-08-13).
- [x] 7.2 [FE-NEXT] `pnpm lint`, `pnpm build`, `pnpm test` pass.
  - GATE: `pnpm lint` PASS (0 errors / 4 pre-existing warnings — matches baseline); `pnpm build` PASS; `pnpm test` PASS (56 files / 292 tests — baseline 52/264 + 28 new: mfa-limiter 9, mfa actions 10, authenticate page 3, security-section 3, mfa-policy-section 3) (2026-08-13).
- [x] 7.3 [OPS-GOV] `openspec validate mfa-totp-enrollment` passes; confirm no local storage of secrets/codes and that the policy mirror is display-only (code review).
  - GATE: `openspec validate mfa-totp-enrollment` PASS (2026-08-13).
  - Code review (SSOT): (a) No local storage of MFA material — TOTP secrets, recovery codes, and QR codes flow only between the Stytch client and transient client-component React state in `security-section.tsx` (cleared on navigation); the server actions never write them to cookies, localStorage, or the DB; audit rows carry only `{type, member_id, organization_id, detail}` with `detail` drawn from the bounded `AUTH_AUDIT_DETAILS` enum (unmapped errors collapse to `internal_error`); session cookies receive only Stytch-issued session_token/session_jwt on successful auth (established pattern). (b) Policy mirror display-only — `mfa-policy-section.tsx` reads `organizationMfaPolicy`/`organizationMfaMethods`/`organizationAllowedMfaMethods` only to seed the form and display; no code path consults the mirror to gate access; Stytch remains the sole MFA enforcement point (enforced at session mint); the write path always goes through the Go backend (`PUT /api/organizations/mfa-policy`, org:manage + circuit breaker).
- [ ] 7.4 [FE-NEXT] E2E (Stytch test project): REQUIRED_FOR_ALL login completes via TOTP; recovery-code sign-in completes in one exchange; duplicate-enrollment retry creates no second instance (record Stytch multi-registration semantics outcome here).
  - **Deferred (blocked):** requires the live Stytch test project with an org configured `mfa_policy: REQUIRED_FOR_ALL` + a member with a seeded TOTP instance and a test-side RFC 6238 TOTP code generator (the repo's E2E harness runs in `AUTH_MOCK_ENABLED` mode, which bypasses Stytch entirely — `getMemberSession` returns a mock session, so it cannot exercise the real `consumeMagicLink` → `authenticateTotp` contract). The unit layer already pins the contracts: mocked-`recoveryCodes.recover` returns the session directly with NO chained `totps.authenticate` (mfa.test.ts), and `createTotp` performs no create call when `totp_registration_id` exists. When test-project access with an MFA-enabled org is available, run the E2E and record the Stytch multi-registration semantics outcome here.

## Phase 0 baseline checkpoint (2026-08-11, repo-wide active-changes run)

- [x] Repo-wide baseline recorded BEFORE further implementation work on this change (working tree: ~330 modified files across both apps from sibling in-flight changes):
  - `go build ./...` PASS (exit 0) · `go vet ./...` PASS · `go test ./...` PASS (all packages, exit 0) — go-b2b-starter
  - `npx tsc --noEmit` PASS · `pnpm lint` PASS (0 errors / 4 pre-existing warnings) · `pnpm build` PASS — next_b2b_starter
  - Context: this baseline anchors later verification gates — failures introduced by this change are distinguishable from pre-existing tree state.
