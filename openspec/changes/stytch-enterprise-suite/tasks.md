# Stytch Enterprise Suite — Tasks

## 1. Stytch Project Configuration (test project)

- [ ] 1.1 [OPS-GOV] In the Stytch test project: enable the SSO, SCIM, OAuth (Google + Microsoft), and Email OTP products; confirm billing verification so magic links can be sent to arbitrary customer emails (record the `billing_not_verified_for_email` prerequisite outcome in this task). Verification: dashboard toggles enabled; a magic link to a non-project-domain address sends successfully in the test project.
- [ ] 1.2 [OPS-GOV] Register Google and Microsoft OAuth providers in the Stytch dashboard (client ID/secret from the provider consoles); confirm the Stytch callback URLs are added to each provider app. Verification: OAuth buttons render in the Stytch B2B component in the test project and complete a real Google/Microsoft sign-in.
- [ ] 1.3 [OPS-GOV] Confirm discovery "Create Organizations" remains disabled in the dashboard; confirm the Stytch B2B component can be configured with org-creation hidden (via `StytchProjectConfigurationInput`). Verification: the discovery flow shows no create-organization action; platform `POST /auth/signup` still bootstraps orgs.

## 2. Backend Auth-Policy Seam

- [x] 2.1 [BE-DOMAIN] Add `OrgAuthPolicyUpdater` interface to `organizations/domain` (`GetAuthPolicy(ctx, orgID)` / `UpdateAuthPolicy(ctx, orgID, emailJitPolicy, allowedDomains, allowedAuthMethods, ssoJitPolicy, ssoJitAllowedConnections, ssoDefaultConnectionID)`) with strict types: `JitPolicy = DISABLED | DOMAIN_RESTRICTED` (maps to Stytch `NOT_ALLOWED`/`RESTRICTED`), `SsoJitPolicy = DISABLED | CONNECTION_RESTRICTED` (maps to `NOT_ALLOWED`/`RESTRICTED` + `sso_jit_provisioning_allowed_connections`), `AllowedAuthMethod = magic_link | email_otp | sso | google_oauth | microsoft_oauth` (SDK-accurate values; NO `email_magic_link`, generic `oauth`, `passkeys`, or `m2m`), plus `ValidateAuthPolicyUpdate` (enum checks, non-empty domain allowlist when `DOMAIN_RESTRICTED`, at least one primary method, no duplicates, and `sso_default_connection_id` must reference an org-owned connection). The write contract includes `auth_methods: RESTRICTED` (enforced-list mode — required for `allowed_auth_methods` to take effect). Mirror `domain/mfa_policy.go` naming/error conventions (`ErrInvalidAuthPolicy`, `ErrAuthPolicyUnavailable`). Verification: `make build`; `go test ./internal/modules/organizations/...` passes. — **DONE**: `domain/auth_policy.go` (types, `AuthPolicy` display-only mirror, `OrgAuthPolicyUpdater`, `ValidateAuthPolicyUpdate` with `activeConnectionIDs` for the `sso_default_connection_id` ownership check); errors added to `domain/errors.go`; `make build` ✓; `go test ./internal/modules/organizations/...` ✓.
- [x] 2.2 [BE-INFRA] Implement in the Stytch auth adapter: `Organizations.Update` with `email_jit_provisioning`, `email_allowed_domains`, `auth_methods: RESTRICTED`, `allowed_auth_methods`, `sso_jit_provisioning`, `sso_jit_provisioning_allowed_connections`, `sso_default_connection_id`, behind the existing two-tier circuit breaker (`Client.Run`); map breaker-open/5xx to 503 structured error `auth_policy_update_unavailable`, 4xx surfaces as a normal error. Implement `GetAuthPolicy` reading the org object (display-only mirror) and mapping breaker-open/unreachable to the 503 structured-error family (`auth_policy_unavailable`). Preserve existing orgs' default method set on first write: when an org's `auth_methods` is `ALL_ALLOWED`, the first write persists `RESTRICTED` + the org's current effective method set + requested additions. Verification: `make build`; adapter unit tests (happy path body asserts — including `auth_methods: RESTRICTED` and correct `allowed_auth_methods` values, 4xx, 5xx → 503 sentinel, breaker-open → 503 + calls stop, read-path breaker-open → 503, validation, first-write default preservation) pass. — **DONE**: `GetAuthPolicy`/`UpdateAuthPolicy` in `stytch_organization_repository.go` (breaker-guarded read-for-preservation + write; `auth_methods: RESTRICTED` always written; `mapToAuthPolicy`/`mapJitPolicy`/`mapSsoJitPolicy`/`mapAllowedAuthMethods`/`computeAllowedAuthMethods`; provider defaults collapse safely). **First-write preserved set (residual-note decision):** the full effective project-enabled set relevant to the org — `magic_link`, `email_otp`, `google_oauth`, `microsoft_oauth`, plus `sso` when the org has ≥1 active connection — not just the `magic_link` floor (documented in code + `TestUpdateAuthPolicyFirstWritePreservesEffectiveMethods`). **SSO-JIT default observation (flagged):** the SDK's `sso_jit_provisioning` docstring documents `ALL_ALLOWED` as the provider default; the platform never writes `ALL_ALLOWED` and the mirror collapses it to `DISABLED` ("not governed") — E2E 7.3a should verify the test project's actual org-create default. All unit tests ✓.
- [x] 2.3 [BE-INFRA] Expose `GET /api/organizations/auth-policy` and `PUT /api/organizations/auth-policy` (authenticated, `org:manage`) delegating to the service, mirroring the `mfa-policy` route shape. Verification: `make build`; handler tests with mock auth (200 read, 200 update, 401 unauthenticated, 403 non-`org:manage`, 503 breaker-open on both read and write) pass; `go test ./internal/modules/organizations/...` passes. — **DONE**: `auth_policy_handler.go` (GET mirror + PUT full-policy write; structured 503 codes `auth_policy_unavailable` / `auth_policy_update_unavailable`; `ErrInvalidAuthPolicy` → 400); routes registered in `routes.go`; service methods + DI wiring in `module.go`; handler tests in `auth_policy_handler_test.go` covering all listed cases ✓; `make build` ✓; `go test ./internal/modules/organizations/...` ✓.

## 3. Frontend — Admin Portal SSO/SCIM Views

- [ ] 3.1 [FE-NEXT] Register `?view=sso` and `?view=scim` in `settings-content.tsx` view routing + the existing view-allowlist gate (`org:manage`), linked from the settings overview, per the `settings-ui` view-command pattern. Verification: `pnpm lint`; `pnpm build` passes; non-`org:manage` users get no entries/reachability.
- [ ] 3.2 [FE-NEXT] **Admin Portal availability gate (decision recorded here):** verify `AdminPortalSSOMountOptions` / `AdminPortalSCIMMountOptions` / `client.portal` exist in the installed `@stytch/vanilla-js` (5.41.0 — NOT found in the type surface during council review) and, if a bump is required, pin and review the new `@stytch/vanilla-js` (and `@stytch/nextjs` if needed) version, updating the "Dependencies" claim in `proposal.md`/design D4; if unavailable in any reviewed version, implement the documented fallback (Go-backed SSO/SCIM connection CRUD, design D4 Branch B) and revise the `enterprise-sso`/`scim-provisioning`/`crm-frontend` deltas before FE sizing. On Branch A, render `AdminPortalSSOMountOptions` on `/settings?view=sso` and `AdminPortalSCIMMountOptions` on `/settings?view=scim`, verify mount option prop shape against the pinned version, and configure `locale: "es"` + strings overrides on the mounts (D8). Verification: `pnpm build`; component tests assert the mounts render for `org:manage` and are absent otherwise; the availability/decision outcome is recorded in this task.
- [ ] 3.3 [FE-NEXT] Add settings-overview cards/entries for SSO and SCIM with descriptions and `org:manage` visibility, consistent with existing overview cards (access/members/subscription/audit). Verification: `pnpm build`; component tests cover visibility for admin vs non-admin.

## 4. Frontend — Login Surface

- [ ] 4.1 [FE-NEXT] Replace the custom email form in `app/auth/page.tsx` with the pre-built `<StytchB2B />` component (Discovery mode, org creation hidden via project config, `locale: "es"` + any required strings overrides per D8). Configure `@stytch/nextjs/b2b` UI client with the enabled methods (magic link, OAuth, email OTP, passkeys per `passkeys-sign-in`). Verification: `pnpm lint`; `pnpm build` passes; `/auth` renders the Stytch component in Spanish with no custom form markup.
- [ ] 4.2 [FE-NEXT] Retire the custom `/auth` pre-validation path: remove `check-email` usage and `sendMagicLink` from the login page (audit remaining callers of `sendMagicLink`/`check-email` first — keep the action/endpoint only if other callers remain, otherwise delete; record the audit outcome in this task). The `auth-email-check` delta in this change assumes retention (endpoint contract kept, auth-page mandate removed); if the audit deletes the endpoint, remove its requirement 1 via a follow-up spec change. Verification: `pnpm build`; `pnpm test` passes with callers reconciled; `auth-email-check` delta consistent with the audit outcome.
- [ ] 4.3 [FE-NEXT] Confirm `/authenticate` continuity: magic-link token consumption, TOTP MFA challenge (`mfa-totp-enrollment`), and passkey continuation (`passkeys-sign-in`) still work with the pre-built login surface (unit/page tests updated where the login page was asserted). Verification: `pnpm test` passes including authenticate-page tests.

## 5. Frontend — JIT Policy UI

- [ ] 5.1 [FE-NEXT] Add a JIT/domain-join card to settings `?view=access`: read current policy from `GET /api/organizations/auth-policy` (display-only mirror), toggle "Allow teammates with @<domain> to join automatically" (domain list input), POST to `PUT /api/organizations/auth-policy`; gate by `org:manage`; render structured 503 error state on breaker-open for both read (`auth_policy_unavailable`) and write (`auth_policy_update_unavailable`). When the org has ≥1 active SSO connection, the card SHALL also render the SSO-JIT toggle (enables `sso_jit_provisioning: RESTRICTED` scoped to the org's connection ids; disables to `NOT_ALLOWED`) per the `enterprise-sso` delta. The UI copy SHALL state that domain JIT also enables OAuth JIT for verified provider emails on the allowed domains. Verification: `pnpm build`; component tests cover read, save, non-admin hidden, 503 read/write error states, SSO-JIT toggle.
- [ ] 5.2 [FE-NEXT] Unit tests for the JIT card with mocked API: disabled default renders correctly, domain-restricted save posts the expected payload (including `auth_methods: RESTRICTED` + complete `allowed_auth_methods`), SSO-JIT toggle posts `sso_jit_provisioning: RESTRICTED` + connection ids, save failure shows structured error and no optimistic state change. Verification: `pnpm test -- access` (or the settings test scope) passes.

## 6. Docs & Governance Reconciliation

- [ ] 6.1 [OPS-GOV] Update `STYTCH_CONFIGURATION.md`: pre-built login + discovery configuration, OAuth/email-OTP/SSO/SCIM enablement, JIT per-org opt-in semantics (including the OAuth-JIT implication of domain JIT), the SSO/SCIM 5-connection budget ($125/conn/mo beyond, pass-through posture), billing-verification prerequisite, branding note (Stytch-branded emails accepted; custom branding is $99/mo), and the honest email-spam/abuse posture change: the pre-built login sends discovery emails to any submitted address, the in-process `magic-link-limiter.ts` no longer bounds login-path sends, and Stytch's per-email rate limit is the sole backstop (org-existence concealment preserved). Verification: doc review; references match implemented behavior.
- [ ] 6.2 [OPS-GOV] `openspec validate stytch-enterprise-suite` passes; confirm the `crm-frontend`, `stytch-nextjs-components`, `auth-email-check`, `stytch-login-surface`, and `enterprise-sso` deltas are consistent with each other (login pre-built, members custom, SSO/SCIM Admin Portal subject to the task-3.2 gate, auth-email-check re-scoped to remaining callers). Verification: `openspec validate` exit 0; manual cross-check of the deltas.
- [ ] 6.3 [OPS-GOV] Re-verify the Stytch pricing figures against a dated source (stytch.com/pricing, recorded date): 10K MAU free, 5 SSO+SCIM connections free per project, $125/conn/mo beyond, Audit Logs $799/mo, branding $99/mo. Confirm the commercial posture in design `## Market & Unit Economics` (pass-through of the $125/conn/mo overage into the Enterprise tier; MAU-headroom watch item for orgs approaching 10K authenticating members/month; discovery-send email volume within the free allowance) and record any drift in the design's Assumptions. Verification: dated source recorded in the task and matching figures in `design.md`; no drift, or drift reconciled.

## 7. Verification Gate

- [ ] 7.1 [BE-INFRA] `make build`; `go test ./internal/modules/organizations/...` and adapter tests pass; full `go test ./...` passes. Verification: all commands exit 0.
- [ ] 7.2 [FE-NEXT] `pnpm lint`, `pnpm build`, `pnpm test` pass. Verification: all commands exit 0.
- [ ] 7.3 [OPS-GOV] E2E in the Stytch test project: (a) SSO — create a SAML/OIDC connection via the Admin Portal (or the task-3.2 fallback), authenticate through it, enable SSO JIT via the auth-policy surface, and confirm JIT provisions a new member with the default `stytch_member` role via `sso_jit_provisioning: RESTRICTED` + the org's connection ids; (b) OAuth — Google sign-in completes; (c) email OTP — member signs in with a one-time code; (d) JIT join — org with domain-restricted JIT admits a matching-domain non-member; non-opted-in orgs do not surface; (e) discovery — no create-organization action, org picker works for multi-org members, and submitting an unknown email surfaces no joinable org (per-email rate limiting observed); (f) first auth-policy write preserves an existing org's current method set. Record outcomes (including SCIM group→role mapping default) in this task. Verification: all E2E flows pass; `openspec validate` passes.
- [ ] 7.4 [OPS-GOV] Confirm no local credential storage: grep the diff for SSO certs/secrets, SCIM bearer tokens, or OTP material — none stored in the local DB; auth-policy mirror is display-only (code review). Verification: grep + review recorded in this task.
