## Sequencing

This change SHALL be applied AFTER `add-council-market-read-gate` (its `council_required()`/persona edits are upstream of this change's REJECTED-branch loop; both touch `council.md`/`pipeline.sh`/`iso.md`/`AGENTS.md` and the `native-agent-pipeline` delta spec). Rebase onto the market-read change before implementing.

## 1. Revise Stage Prompt Template [OPS-GOV]

- [x] 1.1 Add `.pi/prompts/revise.md` — a headless stage template that: (a) reads `openspec/changes/<change>/VERDICT.md`, extracts the numbered "Required design changes" list as the authoritative input; (b) reads `.revise-context.json` (written by `pipeline.sh` from `openspec status --change <name> --json` — artifact `existingOutputPaths` + `changeRoot`) and applies opsx-update's reconciliation rules to those paths — revise in ANY direction, only files that already exist, never create new artifacts; the template SHALL NOT run any `openspec` CLI command (the agent has no bash); (c) overrides opsx-update's interactive confirmation with a headless directive (precedent: `council.md`); (d) writes the machine-parseable `revision.md` sidecar covering each numbered item (`- [x]` addressed / `- [ ]` residual) and the artifacts revised; (e) NEVER edits application source code or files outside the change directory. Verification: read-through confirms every element; pipeline-driven smoke test on a disposable REJECTED change dir (`pipeline.sh` writes `.revise-context.json`, then `pi -p --prompt-template .pi/prompts/revise.md "<change> (verdict-driven revision pass 1)" --tools read,write --approve`) produces a `revision.md` and no code edits.

## 2. Council Verdict Contract Pin [OPS-GOV]

- [x] 2.1 Pin the numbered-required-changes contract AND add the conditional re-review read clause in `.pi/prompts/council.md`: the Deliverable section SHALL state that a `STATUS: REJECTED` verdict MUST include a numbered "Required design changes" list (the revise stage consumes it); the "Your role" section SHALL direct the council — if `revision.md` and a prior `VERDICT.md` exist in the change directory, read them before evaluating the revised `design.md` (baked into the template, not the appended instruction, because the template's strong prohibitions absorb appended text). Marker format, personas, and prohibitions unchanged. Verification: grep of `council.md` shows the pinned sentence and the conditional clause; marker contract untouched.

## 3. pipeline.sh Flags and Parsing [OPS-GOV]

- [x] 3.1 Add `--skip-revise` (REJECTED → immediate exit 2, today's behavior), `--max-revisions <N>` (positive integer; default 2), and `--reset-revisions` (delete `revision.json` before execution; dry-run prints "would delete" instead of deleting; `revision.md` preserved) to `parse_args`, plus `PIPELINE_MAX_REVISIONS` env fallback for the cap. Update the usage/help text and the header comment. Verification: `bash -n scripts/pipeline.sh`; `scripts/pipeline.sh <change> --help` lists all three flags; invalid `--max-revisions 0` exits non-zero with a usage error; `--dry-run --reset-revisions` prints "would delete revision.json" and deletes nothing.

## 4. Bounded Council-Revision Loop [OPS-GOV]

- [x] 4.1 Replace the REJECTED branch of the council gate in `main()`: loop up to `MAX_COUNCIL_REVISIONS` of (write `.revise-context.json` from `openspec status --change <name> --json` → `run_stage revise "<change> (verdict-driven revision pass N)" "read,write"` → remove `.revise-context.json` → `openspec validate <change>` (failure → exit 1) → `run_stage council "Re-review the REVISED design of OpenSpec change <change> (pass N). Write VERDICT.md." "read,write"`) → `parse_verdict`; APPROVED deletes `revision.json` and breaks to sdet; REJECTED after the cap halts exit 2 with a halt message including revision history and log paths; INCONCLUSIVE halts exit 3 (unchanged). `--dry-run` prints the revise/re-review command list (no execution). `parse_verdict` and the marker contract are NOT modified. Verification: `bash -n`; fixture-driven run: a change dir with a REJECTED `VERDICT.md` fixture and a stub `pi` on PATH (or `--dry-run` command-list inspection) shows pass-1 context-file write + revise + validate + re-review, and halts exit 2 after the cap; an APPROVED re-review fixture leaves no `revision.json`; `--skip-revise` on the same fixture exits 2 immediately.

## 5. Revision State Persistence [OPS-GOV]

- [x] 5.1 Add `revision.json` read/write (`{"change": "<name>", "revisions": N, "last_verdict": "...", "updated_at": "<ts>"}`) in the change directory: the loop reads it before each pass and writes it after each pass (jq is a declared prerequisite); a re-run resumes from the recorded count instead of restarting, so the cap is enforced across invocations. Architect stage does NOT pre-create the sidecar. Lifecycle: created by the first revise pass; deleted by the loop on APPROVED; deleted by `--reset-revisions`; never deleted otherwise. Verification: two sequential invocations on a fixture change dir show `revisions` incrementing 0→1→2 and a third run halting at the cap without exceeding it; an APPROVED re-review removes the sidecar; capped halt → `--reset-revisions` → re-run starts a fresh cycle from 0; `revision.md` survives the reset.

## 6. Logging and Traceability [OPS-GOV]

- [x] 6.1 Ensure each revise pass and council re-review lands a distinct log under `logs/pipeline/` (existing `run_stage` timestamped naming; pass number carried in the instruction string) and that halt messages reference the revision history + log paths. Verification: fixture run produces `logs/pipeline/<change>-revise-*-*.log` and `logs/pipeline/<change>-council-*-*.log` files; halt message prints them.

- [x] 6.2 Update `.pi/prompts/iso.md` to record the revision count and final verdict in `docs/compliance/ISO_TRACEABILITY_MATRIX.md`, and update the `AGENTS.md` "Agent Pipeline" section with the `revise` stage (thin wrapper over opsx-update, verdict-driven, never edits code) and the bounded loop (REJECTED → up to `MAX_COUNCIL_REVISIONS` revise→re-review passes → halt exit 2; `--skip-revise`/`--max-revisions`). Verification: `iso.md` and `AGENTS.md` document the loop; no OpenSpec schema or marker-contract text altered.

## 7. Verification Gate [OPS-GOV]

- [x] 7.1 Run `bash -n scripts/pipeline.sh`, `scripts/pipeline.sh <change> --dry-run` (prints revise/re-review commands), `scripts/pipeline.sh <change> --skip-revise --dry-run`, `scripts/pipeline.sh <change> --reset-revisions --dry-run` (prints "would delete revision.json", deletes nothing), existing `parse_verdict` fixture checks (scripts/tests/fixtures/), and shellcheck (if available) — all pass. `openspec validate use-inout-council-for-a-better-design` passes. `.revise-context.json` is transient: present only during a revise pass, absent before/after the pipeline runs.
- [x] 7.2 Record verification results and the archive decision in `tasks.md`.

---

## Verification results (recorded at gate time, 2026-08-12)

- 7.1 gate run: `bash -n scripts/pipeline.sh` PASS; `scripts/pipeline.sh <change> --dry-run` with a REJECTED `VERDICT.md` fixture + `requires_council: true` prints revise pass 1 → `openspec validate` → council re-review → revise pass 2 → validate → council re-review (default cap 2) and executes nothing; `--skip-revise --dry-run` prints zero revise commands; `--reset-revisions --dry-run` prints "would delete .../revision.json" and deletes nothing (file still present); `scripts/tests/test-verdict-parse.sh` PASS (4/4 fixtures, marker contract untouched); shellcheck -S warning PASS; `openspec validate use-inout-council-for-a-better-design` PASS (exit 0); no `.revise-context.json` present before/after runs (transient only).
- End-to-end stub-`pi` fixture runs (disposable `fixture-loop` change, then removed): (a) REJECTED first-pass → revise pass 1 (with `--tools read,write` + transient `.revise-context.json`) → `openspec validate` → re-review APPROVED → `revision.json` cleared, `revision.md` preserved, exit 0; (b) always-REJECTED stub → pass 1 REJECTED, pass 2 REJECTED → cap (2) exhausted → halt exit 2 with revision history (`change=fixture-loop revisions=2 last_verdict=REJECTED`) + log paths; (c) re-run resumed from recorded count=2 → immediate halt, cap not exceeded across invocations; (d) `--skip-revise` → immediate halt exit 2, zero revise invocations; (e) distinct `logs/pipeline/<change>-revise-*.log` / `-council-*.log` per pass. `--max-revisions 0` exits non-zero with a usage error.
- 7.2 Archive decision: **Archive deferred** — `add-council-market-read-gate` (this change's upstream prerequisite, applied first per the sequencing note) is likewise ready; archive both changes together after the next repo-wide verification run, and note that the fixture-change verification artifacts were removed after testing (no test residue in `openspec/changes/`).
