# Tasks: Knowledge Base v2 — permisos por documento (ACL) + rail de dos modos

Implementación del modelo de visibilidad por documento con ACL en retrieval (backend Go + SQLC) y la UI de rail de dos modos (frontend Next). Orden: DB → backend → frontend → verificación.

## 1. Base de datos y SQLC [DB-SQLC]

- [x] 1.1 Crear migración `add_documents_visibility`: columna `visibility` en `documents.documents` (`TEXT NOT NULL DEFAULT 'workspace'`, check `IN ('workspace','admin_only')`) + backfill. Verify: `make migrate` (o el comando de migraciones del repo) sin errores; columna presente. **Hecho:** `000039_add_documents_visibility.up/down.sql`; `NOT NULL DEFAULT 'workspace'` backfillea en el mismo ALTER (metadata-only en PG ≥11); CHECK constraint; down = drop columna. SQLC regenerado con `sqlc generate` (params `include_admin_only`, `visibility`).
- [x] 1.2 Actualizar `documents.sql`: `ListDocumentsByOrganization`/`GetDocumentByID`/`UpdateDocument` con filtro/param de `visibility` según rol del miembro. Verify: `make sqlc` sin diff manual en gen. **Hecho:** filtro `(visibility = 'workspace' OR include_admin_only)` en list/status/count/get; `UpdateDocument` con `visibility = COALESCE(NULLIF(...), visibility)`; `sqlc generate` sin diff posterior.
- [x] 1.3 Actualizar `cognitive.sql`: `SearchSimilarDocuments` con ACL de visibilidad (JOIN a documentos + condición por rol admin). Verify: `make sqlc`; query regenerada. **Hecho:** JOIN `documents.documents` con `(d.visibility = 'workspace' OR include_admin_only)` y tenancy `d.organization_id = de.organization_id`; regenerado.

## 2. Backend Go [BE-DOMAIN]

- [x] 2.1 Spike: confirmar que el rol efectivo (`org:manage`) está disponible en el contexto del servicio RAG y en los handlers de documentos; mapear todos los consumers de `SearchSimilarDocuments`. Verify: notas en tasks; grep de consumers completo. **Spike (notas):** (1) Rol disponible: `authcontext.RequestContext.Identity.Permissions` (poblado por middleware auth); handlers y chat RAG computan `HasPermission(org:manage)`. (2) Consumers de `SearchSimilarDocuments`: SOLO `ragService.Chat` (rag_service.go:91) y `ragService.ChatStream` (rag_service.go:199) vía `embeddingRepo.SearchSimilar`; `embeddingService.SearchSimilarDocuments` no tiene consumers externos (interface únicamente). `GetDocumentEmbeddingsByDocumentID`/`GetDocumentByFileAssetID` son paths internos de procesamiento (no user-facing). El filtro vive en la query SQL compartida.
- [x] 2.2 Domain/adapters: propagar `visibility` en create/update/list/get de documentos y en la búsqueda RAG (filtro por rol del miembro solicitante). Verify: `make test`; unit tests de ACL. **Hecho:** `domain.DocumentVisibility` + `Document.Visibility`; repo `GetByID/List/ListByStatus/Count/CountByStatus` con `includeAdminOnly`; `SearchSimilar` con `includeAdminOnly`; `ragService.Chat/ChatStream` lo reciben del handler. Tests: `embedding_service_test.go` (forwarding ACL), `handler_test.go` (404/200/400).
- [x] 2.3 Endpoints de documentos: `visibility` en request/response de update; list/get aplican el filtro (doc restringido = inexistente para miembro sin permiso). Verify: `make test`; tests de 404 sin fuga de título. **Hecho:** `GET /example_documents/:id` (nuevo, filtra por visibilidad → 404 sin fuga de título), `PATCH /example_documents/:id` (title/visibility); repos mapean `pgx.ErrNoRows` → `ErrDocumentNotFound` → 404.
- [x] 2.4 Export compliance (Ley 1581): incluir lista de documentos indexados con título/estado/visibilidad sin romper contrato actual. Verify: `make test`; contrato previo intacto. **Hecho:** `GET /example_documents/export/compliance.csv` (org:manage) vía `ListIndexedDocuments` (docs con embeddings, EXISTS a `cognitive.document_embeddings`); CSV con BOM UTF-8 y escape anti-formula; contrato de export de contacto (agent, Habeas Data) intacto. Test: `TestExportComplianceStreamsCSV` + gate 403.
- [x] 2.5 Gate admin-only: upload/delete/rename/visibility requieren `org:manage` (verificación server-side, no solo UI). Verify: `make test`; test de 403. **Hecho:** rutas: upload/patch/delete → `RequirePermissionFunc("org","manage")`; list/get → `resource:view` con filtro server-side. Tests `TestUpdateRouteRequiresOrgManage`/`TestExportComplianceRejectsMemberWithoutOrgManage` (403).

## 3. Frontend [FE-NEXT]

- [x] 3.1 Rail de dos modos: `?mode=chat|docs` en URL, pane principal alterna thread ⇄ detalle, contexto xl+ (drawer <1280px, oculto móvil), mismo frame que bandeja. Verify: `pnpm build`; modos navegables con URL. **Hecho:** `knowledge-content.tsx` orquesta modos vía `useSearchParams`/`router.replace` (deep-link/refresh/back); rail segmentado en `knowledge-sidebar.tsx`; pane de contexto xl+ (drawer <1280px vía toggle, oculto en móvil); `pnpm build` PASS.
- [x] 3.2 Chat con citas: marcadores `[n]` inline, tarjetas de cita (título/icono/score legible), copiar, salto al chunk en detalle; fallback "Documento no disponible"; estado honesto "No encontré esto en tus documentos". Verify: `npx vitest run app/dashboard/knowledge/components/chat-message.test.tsx app/dashboard/knowledge/components/document-sources.test.tsx`; `pnpm build`. **Hecho:** backend emite `referenced_docs` en el evento SSE `done` (handler.go) + `use-chat-stream` los parsea; `[n]` → chips `#fuente-n` (markdown.tsx `citationCount`); tarjetas con `getSimilarityLabel` ("92% coincidencia") y salto a `?mode=docs&doc=<id>`; honest guard anti-hallucination en `rag_service.go` (respuesta determinista sin chunks). Tests knowledge PASS.
- [x] 3.3 Docs: chips de estado (procesando/lista/error+Retry re-embedding), detalle con metadata + visibilidad, Reprocesar/Renombrar/Eliminar con ConfirmDialog; chip "N en indexación" en header del chat. Verify: `npx vitest run app/dashboard/knowledge/components/document-upload.test.tsx`; `pnpm build`. **Hecho:** endpoint backend `POST /example_documents/:id/reprocess` (re-embedding, nunca re-subida); `document-detail.tsx` con metadata/visibilidad y ConfirmDialogs; chip "N en indexación" en header del chat; chips de estado con texto (nunca color-only).
- [x] 3.4 Visibilidad en UI: badge lock en docs `admin_only` (admins), estado "Documento restringido" sin título para miembro sin permiso; upload/delete ocultos para no-admin. Verify: `pnpm build`; 403 sin fuga de título. **Hecho:** badge lock + label en cards/detalle; `RestrictedDocumentState` (sin título) cuando el doc no está en la lista visible del miembro; upload/delete/rename/visibility renderizados solo con `org:manage` (`usePermissions`).
- [x] 3.5 Créditos: chip en header (amber ≥80%), composer disabled + banner "Créditos agotados — ver plan" cuando `credits_remaining = 0`, tokens por mensaje en tooltip; upgrade gate completo si `ai_assistant` apagado. Verify: `pnpm build`; estados verificados. **Hecho:** chip con `useAiUsageQuery` (amber ≥80%); composer disabled + banner + link al plan; tooltip de tokens por mensaje; upgrade gate al recibir 403 `funcionalidad_no_disponible` (`use-chat-stream` expone el code).
- [x] 3.6 Empty state guiado: "Agrega tu primer documento" + botón PDF de muestra + explicación; enlace desde checklist de onboarding. Verify: `pnpm build`. **Hecho:** empty state docs con botón de PDF de muestra (base64) + explicación; checklist `first-run-checklist.tsx` con paso "Agrega tu primer documento" → `/dashboard/knowledge` (marca visitado en storage).
- [x] 3.7 Repositorio frontend: `document-repository.ts` con param `visibility`; tipos DTO actualizados. Verify: `pnpm lint`; `npx tsc --noEmit`. **Hecho:** `visibility` en model/DTO/repo (`DocumentVisibility`), `getDocument/updateDocument/reprocessDocument`; `tsc --noEmit` y `pnpm lint` PASS (0 errores).
- [x] 3.8 Copy nueva en `lib/copy/ui.ts` (español tipado: "Base de conocimiento", "Nueva conversación", "Agregar documento", "Procesando…", "Documento restringido", "No encontré esto en tus documentos"). Verify: `pnpm lint`. **Hecho:** sección `knowledge` ampliada (es `ui` + `en`) + onboarding `stepAddDocument`; `pnpm lint` PASS.

## 4. Verificación [OPS-GOV]

- [x] 4.1 Gate backend: `make sqlc` sin diff, `make test` (incluye tests de ACL y 403/404). Verify: comandos PASS. **Hecho:** `sqlc generate` idempotente (sin diff posterior; regen v1.30.0 alineada con el CLI docker del repo, al día con queries existentes); `go build ./...`, `go vet`, `go test ./...` (62 paquetes) PASS; `scripts/run_tests_with_coverage.sh` PASS. Se corrigió además el match wildcard `*:*` (mock admin) en `canManage`/`includeAdminOnly` para alinear con `RequirePermissionFunc`.
- [x] 4.2 Gate estático frontend: `pnpm lint`, `pnpm build`, `npx tsc --noEmit`. Verify: los tres comandos PASS. **Hecho:** los tres PASS (0 errores; 4 warnings pre-existentes en otros archivos).
- [x] 4.3 Gate unitario frontend: `npx vitest run app/dashboard/knowledge/` → todos PASS. Verify: comando PASS. **Hecho:** 10/10 knowledge + suite completa 302+ PASS (crm flake por timeout de carga, pasa en aislamiento, no relacionado).
- [x] 4.4 Gate visual/a11y: capturas Playwright 390x844/768x1024/1440x900 (modos chat/docs, detalle, restringido) → `openspec/changes/knowledge-doc-permissions/qa/`; streaming en live region, citas por teclado, chips con texto. Verify: artefactos en `qa/`. **Hecho:** `e2e/specs/qa-knowledge.spec.ts` (seed doc admin_only, verificación 404 miembro / 200 admin sin fuga de título) → 13 capturas + `qa-report.json`; live-region verificada en chat; chips con texto (nunca color-only); se detectó y corrigió nested-`<button>` (hydration error) en la tarjeta de documento. QA PASS.
- [x] 4.5 Cierre: `openspec validate knowledge-doc-permissions --type change` PASS; registrar decisión de archivado (ejecutar /opsx-archive o anotar `**Archive deferred:** <razón>`). Verify: comando validate PASS. **Hecho:** validate PASS. **Archive deferred:** pendiente de implementar `inbox-member-tier` (mismo lote de cambio); se archivará al cerrar el lote.
