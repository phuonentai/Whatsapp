# Tasks: site-redesign-lean-soft

Rediseño del sitio (más ligero, colores empresariales suaves). Fases: A) tokens/tipografía → B) marketing → C) shell/producto → D) verificación. Frontend only (`next_b2b_starter/`); cero cambios de lógica (auth Stytch, RBAC, billing, datos, rutas).

## 1. Tokens de diseño y tipografía (Fase A) [FE-NEXT]

- [x] 1.1 `app/globals.css`: reemplazar la paleta shadcn light (hue 240, primario negro) por la paleta empresarial suave según design.md D1: `--background` `60 33% 99%`, `--foreground` `222 18% 22%`, `--card` `0 0% 100%`, `--primary` azul corporativo apagado `≈217 42% 46%`, `--secondary` verde salvia `150 28% 93%`, `--muted` `60 12% 96%`, `--border` `220 12% 90%`, `--ring` `217 45% 55%`. Verify: `pnpm build`; inspección visual de tokens
- [x] 1.2 `app/globals.css` `.dark`: variante oscura suave (no negro puro) según D1: `--background` `222 15% 10%`, `--card` `222 13% 13%`, `--primary` `217 55% 62%`, `--secondary` `150 20% 20%`, `--border` `222 12% 22%`. Verify: `pnpm build`; toggle de tema renderiza ambos
- [x] 1.3 Contraste AA del primario azul: verificar ratio del `--primary` propuesto sobre `--card`/blanco; si <4.5:1 ajustar luminosidad (mantener saturación ≤50%) hasta cumplir. Verify: calculadora de contraste; ratio ≥4.5:1 texto normal, ≥3:1 UI
- [x] 1.4 Tipografía Inter-only: `components/marketing/fonts.ts` — retirar `Sora` de `next/font/google`; `tailwind.config.ts` — alias `font-heading` apunta a la variable Inter ya cargada en `app/layout.tsx` (los usos de `font-heading` no cambian). Verify: `pnpm build`; grep sin `Sora` en el repo frontend

## 2. Sitio de marketing ligero (Fase B) [FE-NEXT]

- [x] 2.1 Hero (`components/marketing/hero.tsx`): superficie clara (`bg-background`/`bg-card` con borde sutil) sin gradientes oscuros ni `animate-pulse`; h1 máx. `text-5xl`; badge estático con tintes `secondary`; CTAs `bg-primary` sin `hover:scale` ni `shadow-emerald-*`. Verify: `pnpm build`; `pnpm dev` — hero claro, sin animación de entrada (LCP)
- [x] 2.2 Header/footer (`site-header.tsx`, `site-footer.tsx`): `bg-slate-900` → `bg-background/95 backdrop-blur` con `border-b border-border`; footer claro con bordes suaves (reemplaza el 4-col oscuro); enlaces `text-slate-300` → `text-muted-foreground hover:text-foreground`; CTA del header `bg-emerald-500` → `bg-primary`. Verify: `pnpm build`; navegación pública funcional en `pnpm dev`
- [x] 2.3 Botones/CTAs globales de marketing: barrido `grep -rn "emerald-\|slate-900" components/marketing/` → botones primarios `bg-primary hover:bg-primary/90`, sin `hover:scale`, sin sombras emerald. Verify: `pnpm lint`; `pnpm build`; grep sin emerald-500 en botones
- [x] 2.4 Composición de secciones (`app/(marketing)/page.tsx`, `section-heading.tsx`, `feature-grid.tsx`, `comparison.tsx`): alternancia de superficies `card`/`muted` con `border-y` sutiles; padding `py-20 lg:py-32` → `py-16 lg:py-24`; sin bloques `bg-slate-900`; gradientes pesados eliminados. Verify: `pnpm build`; scroll visual sin secciones oscuras
- [x] 2.5 Pricing/ROI/FAQ (`pricing.tsx`, `roi-calculator.tsx`, `faq.tsx`, `cta-banner.tsx`): restyle con tokens suaves manteniendo lógica (toggle mensual/anual, sliders, accordion); tarjeta "popular" con borde `primary` en vez de emerald. Verify: `pnpm build`; toggle/sliders/accordion operativos en `pnpm dev`
- [x] 2.6 Motion restringido (`reveal.tsx` + usos): fade-up sutil ≤300ms; desactivado por defecto en el hero; respetar `prefers-reduced-motion` (sin animación); sin scroll-jacking. Verify: `pnpm lint`; `pnpm build`; `pnpm dev` con `prefers-reduced-motion` activo
- [x] 2.7 Páginas restantes del sitio público (blog, academy, features, plataforma, pricing, faq, legal, `page-hero.tsx`, `prose.tsx`): barrido de clases `slate-900`/`emerald-*` → tokens suaves; `page-hero` claro. Verify: `pnpm build`; todas las rutas públicas renderizan
- [x] 2.8 Barrido de verificación de marketing: `grep -rn "bg-slate-900\|bg-emerald-500\|shadow-emerald" components/marketing/ app/\(marketing\)/` → sin coincidencias (excepto usos decorativos acotados aprobados). Verify: comando grep; `pnpm lint`; `pnpm build`

## 3. Shell y superficies del producto (Fase C) [FE-NEXT]

- [x] 3.1 Sidebar (`components/layout/sidebar.tsx`): `bg-slate-900`/`border-slate-800` → `bg-card border-r border-border`; ítem activo `bg-primary/10 text-primary`, hover `bg-muted`; textos `text-slate-400/300` → `text-muted-foreground`; **sin cambios** en rutas, gating RBAC ni render condicional de grupos. Verify: `pnpm build`; navegación con permisos intacta
- [x] 3.2 Tarjeta "IA Insights" (sidebar): gradiente emerald → tintes salvia suaves (`bg-secondary/60 border-secondary text-secondary-foreground`); CTA `bg-emerald-500` → `bg-primary`; render condicional RBAC intacto. Verify: `pnpm build`; tarjeta visible solo con permiso
- [x] 3.3 Top bar (`components/layout/header.tsx` + `user-menu.tsx`): `bg-slate-900/95` → `bg-background/95 backdrop-blur`; bordes `border-slate-800` → `border-border`; búsqueda ⌘K, notificaciones y menú usuario sin cambios de comportamiento; badge de notificaciones `bg-emerald-500` → `bg-primary`. Verify: `pnpm build`; palette ⌘K operativa
- [x] 3.4 Overview (`app/dashboard/page.tsx`): **solo clases** — tarjetas KPI/paneles `bg-slate-900`/gradientes emerald → tokens suaves (`bg-card border-border`, panel "Copiloto IA" con `bg-secondary/60`). **Las ramas `verifyPayment`/`verifyMercadoPagoPayment` y sus redirects SHALL quedar sin cambios** — verificar diff. Verify: `npx vitest run app/dashboard/page.test.tsx` (4/4); `git diff app/dashboard/page.tsx` muestra solo cambios de clase; `pnpm build`
- [x] 3.5 Bandeja (`app/dashboard/inbox/*`): barrido de clases oscuras/emerald → tokens (métricas, toolbar, lista, badges); filtros, store y queries intactos. Verify: `npx vitest run app/dashboard/inbox/components/conversation-list.test.tsx`; `pnpm build`
- [x] 3.6 Signup (`app/signup/*`): contenedor del wizard `bg-slate-900`/gradiente oscuro → superficie clara suave de tema; `use-signup-flow.ts` y componentes Stytch sin cambios. Verify: `npx vitest run app/signup/page.test.tsx` (3/3); `pnpm build`
- [x] 3.7 Barrido de producto: `grep -rn "bg-slate-900\|bg-emerald-500" components/layout/ app/dashboard/ app/signup/` → sin superficies oscuras fijas (salvo usos decorativos aprobados). Verify: comando grep; `pnpm lint`; `pnpm build`

## 4. Verificación y cierre (Fase D) [OPS-GOV]

- [x] 4.1 Gate estático: `pnpm lint` (0 errores nuevos vs baseline), `pnpm build` (producción, todas las rutas), `npx tsc --noEmit`. Registrar resultados en este archivo. Verify: los tres comandos PASS
- [x] 4.2 Gate unitario: `npx vitest run app/dashboard/page.test.tsx app/signup/page.test.tsx app/dashboard/inbox/components/conversation-list.test.tsx` → todos PASS (incluye routing de checkout Polar/MercadoPago). Verify: comando PASS
- [ ] 4.3 Gate de retorno de pago (**BLOQUEADO por infraestructura**: backend Go sin `.env`/secretos Stytch/Polar/MP en este entorno; cubierto por unit tests de routing 10/10) (manual/e2e): con `pnpm dev` + backend Go, completar retorno de checkout Polar (`checkout_id`) y MercadoPago (`payment_id`/`preapproval_id`) → aterrizan en `/dashboard/settings?view=subscription` con `payment_verified=true`/`payment_error=true` según el caso. Verify: flujos manuales registrados; e2e `paywall`/checkout si disponible
- [ ] 4.4 Gate e2e Playwright (**BLOQUEADO por infraestructura**: requiere backend Go + orgs sembradas + Stytch; no disponible en este entorno; page-objects sin cambios de lógica): ejecutar specs de dashboard/inbox/signup/CRM/marketing; si un page-object depende de clases de estilo (`bg-slate-900`, textos emerald), actualizar el page-object (no la lógica). Verify: `pnpm exec playwright test` (o el subset disponible) PASS
- [x] 4.5 Gate visual/a11y (stage /uiux): capturas Playwright en 390x844/768x1024/1440x900 → `openspec/changes/site-redesign-lean-soft/qa/`; verificar contraste AA (D4), ambos temas, ausencia de superficies oscuras fijas en tema claro. Verify: artefactos en `qa/`; checklist a11y PASS
- [x] 4.6 Cierre: `openspec validate site-redesign-lean-soft --type change` PASS; registrar decisión de archivado (ejecutar /opsx-archive o anotar `**Archive deferred:** <razón>`). Verify: comando validate PASS


**Archive decision (superseded):** `site-redesign-lean-soft` queda **SUPERSEDIDO por `adopt-verifika-template`** — la dirección visual se revierte a la identidad Verifika (chrome oscuro slate-900 + emerald + contenido claro). El rediseño soft-light de este change fue aplicado y luego reemplazado; los gates 4.3/4.4 permanecen BLOQUEADOS por infraestructura (backend Go sin secretos en este entorno) y se heredan como riesgo conocido en adopt-verifika-template (5.3/5.4). No ejecutar /opsx-archive sobre este change sin antes resolver la supersedencia.
